# Tasks

- [ ] Add addon catalog DTO aliases for `genre`, `year`, `imdb_id`, and `behaviorHints.defaultVideoId`.
- [ ] Map aliases into `MetaPreview` first-paint display fields.
- [ ] Populate `MetaPreview.firstPaintStableIds` from addon preview IDs.
- [ ] Add mapper tests for TMDB search/catalog response shapes.
- [ ] Add repository regression coverage for mixed item types in one addon route.
- [ ] Add architecture guards for no parallel addon lifecycle.
- [ ] Run focused mapper, repository, architecture, and OpenSpec validation commands.
