services:
  shadow-collector:
    build: .
    container_name: nexio-shadow-collector
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    restart: unless-stopped
