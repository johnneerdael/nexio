package com.nexio.build.integrationaudit

import org.gradle.api.Plugin
import org.gradle.api.Project

class IntegrationAuditPlugin : Plugin<Project> {
    override fun apply(project: Project) = Unit
}
