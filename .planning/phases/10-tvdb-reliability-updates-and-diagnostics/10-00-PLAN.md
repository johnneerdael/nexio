---
phase: 10-tvdb-reliability-updates-and-diagnostics
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-BINDINGS.md
  - app/src/main/java/com/nexio/tv/core/tvdb/TvdbReliabilityDiagnostics.kt
  - app/src/test/java/com/nexio/tv/core/tvdb/TvdbReliabilityDiagnosticsTest.kt
autonomous: true
requirements:
  - UX-03
  - CACHE-02
  - CACHE-03
user_setup: []
must_haves:
  truths:
    - "Phase 10 implementation does not start until Phase 6-9 TVDB source files are present and bound to exact class names."
    - "TVDB diagnostics use typed reason codes, sanitized payloads, a shared recorder interface, and structured log fields before update, reference, fallback, or UI work consumes them."
    - "Diagnostics can represent update refresh, stale cache, invalid credentials, provider choice, fallback, date-only gating, missing airsTime, poster override, and skipped TMDB TV fetches."
  artifacts:
    - path: ".planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-BINDINGS.md"
      provides: "Exact Phase 6-9 TVDB source binding for Phase 10 executors"
      contains: "TvdbMetadataRouter.kt"
    - path: "app/src/main/java/com/nexio/tv/core/tvdb/TvdbReliabilityDiagnostics.kt"
      provides: "Typed sanitized diagnostic reasons and events"
      contains: "TvdbReliabilityReason"
  key_links:
    - from: "10-BINDINGS.md"
      to: "Phase 10 implementation plans"
      via: "read_first binding contract"
      pattern: "Phase 10 bound files"
    - from: "TvdbReliabilityDiagnostics.kt"
      to: "all later TVDB reliability services"
      via: "shared reason enum"
      pattern: "TvdbReliabilityReason"
---

<objective>
Bind Phase 10 to the actual Phase 6-9 TVDB implementation and create the shared diagnostic contract.

Purpose: Phase 10 depends on TVDB provider, timing, and advanced-surface code that may not exist in the current checkout. This plan prevents shallow execution against guessed classes and gives later plans one sanitized diagnostic vocabulary.
Output: `10-BINDINGS.md`, `TvdbReliabilityDiagnostics.kt`, and diagnostics unit coverage.
</objective>

<execution_context>
@/Users/jneerdael/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jneerdael/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-CONTEXT.md
@.planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-RESEARCH.md
@.planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-VALIDATION.md
@.planning/phases/06-tvdb-foundation-and-identity/06-01-PLAN.md
@.planning/phases/07-tvdb-provider-replacement/07-01-PLAN.md
@.planning/phases/07-tvdb-provider-replacement/07-02-PLAN.md
@.planning/phases/08-exact-continue-watching-air-timing/08-01-PLAN.md
@.planning/phases/09-tvdb-advanced-tv-surfaces/09-00-PLAN.md
@tvdb.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate and bind Phase 6-9 TVDB source contracts</name>
  <files>.planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-BINDINGS.md</files>
  <read_first>
    .planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-CONTEXT.md,
    .planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-RESEARCH.md,
    .planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-VALIDATION.md,
    .planning/phases/07-tvdb-provider-replacement/07-01-PLAN.md,
    .planning/phases/07-tvdb-provider-replacement/07-02-PLAN.md,
    .planning/phases/08-exact-continue-watching-air-timing/08-01-PLAN.md,
    .planning/phases/09-tvdb-advanced-tv-surfaces/09-00-PLAN.md,
    tvdb.yml
  </read_first>
  <action>
Before editing app source, run these exact checks:

`test -f app/src/main/java/com/nexio/tv/data/local/TvdbSettingsDataStore.kt`
`test -f app/src/main/java/com/nexio/tv/data/local/TvdbTokenStore.kt`
`test -f app/src/main/java/com/nexio/tv/data/remote/api/TvdbApi.kt`
`test -f app/src/main/java/com/nexio/tv/core/tvdb/TvdbAuthService.kt`
`test -f app/src/main/java/com/nexio/tv/core/tvdb/TvdbIdentityService.kt`
`test -f app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataModels.kt`
`test -f app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataDiagnostics.kt`
`test -f app/src/main/java/com/nexio/tv/core/tvdb/TvdbMetadataService.kt`
`test -f app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataRouter.kt`
`test -f app/src/main/java/com/nexio/tv/core/tvdb/TvdbAirAvailability.kt`
`test -f app/src/main/java/com/nexio/tv/core/tvdb/TvdbAirAvailabilityCalculator.kt`
`test -f app/src/main/java/com/nexio/tv/ui/screens/settings/TvdbSettingsViewModel.kt`
`test -f app/src/main/java/com/nexio/tv/ui/screens/settings/TvdbSettingsScreen.kt`

Then run `rg -n "class TvMetadataRouter|class TvdbMetadataService|data class TvMetadataEnrichment|enum class TvMetadataDecisionReason|TMDB_TV_SKIPPED|TvdbAirAvailabilityDiagnosticReason|missing_airs_time|date_only_gating|poster_ratings_override" app/src/main/java app/src/test/java`.

If any required file is absent, stop immediately and return `## CHECKPOINT REACHED` with this blocker: "Phase 10 is blocked because Phase 6-9 TVDB source is absent. Execute or merge Phases 6, 7, 8, and 9 first so Phase 10 can bind TvdbSettingsDataStore, TvdbTokenStore, TvdbApi, TvdbAuthService, TvdbIdentityService, TvMetadataModels, TvMetadataDiagnostics, TvdbMetadataService, TvMetadataRouter, TvdbAirAvailability, TvdbAirAvailabilityCalculator, TvdbSettingsViewModel, and TvdbSettingsScreen." Do not create replacement TVDB auth, identity, provider-router, exact-airtime, or advanced-surface code in Phase 10.

If all checks pass, create `10-BINDINGS.md` with a table titled `Phase 10 bound files` containing these columns: `Role`, `Expected file`, `Actual file`, `Required symbol`, and `Bound`. Include rows for `settings_store`, `token_store`, `api`, `auth_service`, `identity_service`, `metadata_models`, `metadata_diagnostics`, `metadata_service`, `metadata_router`, `air_availability`, `air_calculator`, `settings_view_model`, and `settings_screen`. Use `yes` in `Bound` only when the actual file contains the required symbol.

Do not read, print, or mention local TVDB secret files such as `.thetvdb.apikey`.
  </action>
  <verify>
    <automated>test -f app/src/main/java/com/nexio/tv/data/local/TvdbSettingsDataStore.kt && test -f app/src/main/java/com/nexio/tv/data/local/TvdbTokenStore.kt && test -f app/src/main/java/com/nexio/tv/data/remote/api/TvdbApi.kt && test -f app/src/main/java/com/nexio/tv/core/tvdb/TvdbAuthService.kt && test -f app/src/main/java/com/nexio/tv/core/tvdb/TvdbIdentityService.kt && test -f app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataModels.kt && test -f app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataDiagnostics.kt && test -f app/src/main/java/com/nexio/tv/core/tvdb/TvdbMetadataService.kt && test -f app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataRouter.kt && test -f app/src/main/java/com/nexio/tv/core/tvdb/TvdbAirAvailability.kt && test -f app/src/main/java/com/nexio/tv/core/tvdb/TvdbAirAvailabilityCalculator.kt && test -f app/src/main/java/com/nexio/tv/ui/screens/settings/TvdbSettingsViewModel.kt && test -f app/src/main/java/com/nexio/tv/ui/screens/settings/TvdbSettingsScreen.kt</automated>
  </verify>
  <acceptance_criteria>
    - If any required file is missing, the executor returns `## CHECKPOINT REACHED` and the exact blocker string from the action.
    - If all files exist, `10-BINDINGS.md` contains `Phase 10 bound files`.
    - `10-BINDINGS.md` contains rows for `metadata_router`, `air_calculator`, and `settings_screen`.
    - Every row in `10-BINDINGS.md` contains `| yes |` in the `Bound` column before Task 2 starts.
    - The executor does not read or print `.thetvdb.apikey`, API keys, PINs, bearer tokens, or Authorization headers.
  </acceptance_criteria>
  <done>Phase 6-9 source availability is proven and documented, or Phase 10 stops before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create typed sanitized reliability diagnostics</name>
  <files>
    app/src/main/java/com/nexio/tv/core/tvdb/TvdbReliabilityDiagnostics.kt,
    app/src/test/java/com/nexio/tv/core/tvdb/TvdbReliabilityDiagnosticsTest.kt
  </files>
  <read_first>
    .planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-BINDINGS.md,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataDiagnostics.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvdbAirAvailability.kt,
    .planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-CONTEXT.md,
    .planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-VALIDATION.md
  </read_first>
  <behavior>
    - `TvdbReliabilityReason` exposes exact codes for all reasons required by D-11.
    - Diagnostic sanitization removes `Authorization`, `Bearer`, `apikey`, `apiKey`, `pin`, and `token` values.
    - User-facing status is only emitted for invalid credentials, stale cache served, and update/reference refresh failures.
    - Structured logs are emitted from sanitized diagnostic payloads only.
  </behavior>
  <action>
Create `TvdbReliabilityDiagnostics.kt` in package `com.nexio.tv.core.tvdb` per D-10 and D-11 so user-facing status, Debug detail, and logs all share the same typed reason vocabulary.

Add `enum class TvdbReliabilityReason(val code: String)` with these exact values and strings:
- `TVDB_PROVIDER_CHOSEN("tvdb_provider_chosen")`
- `EXPLICIT_FALLBACK("explicit_fallback")`
- `MISSING_AIRS_TIME("missing_airs_time")`
- `DATE_ONLY_GATING("date_only_gating")`
- `POSTER_RATINGS_OVERRIDE("poster_ratings_override")`
- `TMDB_TV_FETCH_SKIPPED("tmdb_tv_fetch_skipped")`
- `UPDATE_REFRESH_STARTED("update_refresh_started")`
- `UPDATE_REFRESH_SUCCEEDED("update_refresh_succeeded")`
- `UPDATE_REFRESH_FAILED("update_refresh_failed")`
- `REFERENCE_REFRESH_SUCCEEDED("reference_refresh_succeeded")`
- `REFERENCE_REFRESH_FAILED("reference_refresh_failed")`
- `STALE_CACHE_SERVED("stale_cache_served")`
- `INVALID_CREDENTIALS("invalid_credentials")`
- `NETWORK_CALL_BLOCKED("network_call_blocked")`
- `UNKNOWN_UPDATE_EVENT("unknown_update_event")`

Add:
```kotlin
data class TvdbReliabilityDiagnostic(
    val reason: TvdbReliabilityReason,
    val surface: String,
    val tvdbId: Int? = null,
    val entityType: String? = null,
    val fallbackProvider: String? = null,
    val message: String? = null,
    val occurredAtMs: Long = System.currentTimeMillis()
)
```

Add `fun TvdbReliabilityDiagnostic.sanitized(): TvdbReliabilityDiagnostic` that replaces secret-looking values in `message` with `[redacted]`. Match case-insensitively on `authorization`, `bearer`, `apikey`, `api_key`, `apiKey`, `pin`, and `token`.

Add `fun TvdbReliabilityDiagnostic.userStatusLine(): String?` returning:
- `"TVDB credentials need attention"` for `INVALID_CREDENTIALS`
- `"TVDB served cached data while refresh is unavailable"` for `STALE_CACHE_SERVED`
- `"TVDB cache refresh failed"` for `UPDATE_REFRESH_FAILED`
- `"TVDB reference refresh failed"` for `REFERENCE_REFRESH_FAILED`
- `null` for all other reasons.

Add the shared producer contract required by D-10:
```kotlin
interface TvdbDiagnosticsRecorder {
    suspend fun record(diagnostic: TvdbReliabilityDiagnostic)
}
```

Add `fun TvdbReliabilityDiagnostic.structuredLogFields(): Map<String, String>` that calls `sanitized()` first and returns only non-secret string fields: `reason`, `surface`, optional `tvdb_id`, optional `entity_type`, optional `fallback_provider`, optional sanitized `message`, and `occurred_at_ms`. Do not include request URLs, headers, API keys, PINs, bearer tokens, or Authorization values. Later update/reference/fallback/credential producers must depend on `TvdbDiagnosticsRecorder` and emit structured logs through this sanitized contract rather than freeform strings.
  </action>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.core.tvdb.TvdbReliabilityDiagnosticsTest"</automated>
  </verify>
  <acceptance_criteria>
    - `TvdbReliabilityDiagnostics.kt` contains `enum class TvdbReliabilityReason`.
    - `TvdbReliabilityDiagnostics.kt` contains `TMDB_TV_FETCH_SKIPPED("tmdb_tv_fetch_skipped")`.
    - `TvdbReliabilityDiagnostics.kt` contains `DATE_ONLY_GATING("date_only_gating")`.
    - `TvdbReliabilityDiagnostics.kt` contains `STALE_CACHE_SERVED("stale_cache_served")`.
    - `TvdbReliabilityDiagnostics.kt` contains `INVALID_CREDENTIALS("invalid_credentials")`.
    - `TvdbReliabilityDiagnostics.kt` contains `interface TvdbDiagnosticsRecorder`.
    - `TvdbReliabilityDiagnostics.kt` contains `structuredLogFields`.
    - `TvdbReliabilityDiagnosticsTest.kt` contains `sanitized diagnostic redacts authorization bearer api key pin and token`.
    - `TvdbReliabilityDiagnosticsTest.kt` contains `all phase ten required reason codes are represented`.
    - `TvdbReliabilityDiagnosticsTest.kt` contains `structured log fields are sanitized`.
    - Targeted Gradle test command exits 0.
  </acceptance_criteria>
  <done>Phase 10 has one typed diagnostics contract with redaction tests before reliability services consume it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase plans -> app source | Executor may otherwise guess absent Phase 6-9 classes and create duplicate TVDB auth/provider code. |
| TVDB diagnostics -> UI/logs | Provider and credential state cross into user-visible Debug/status surfaces and structured logs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-00-01 | Tampering | `10-BINDINGS.md` | mitigate | Gate on exact source files and symbols before any Phase 10 implementation; stop with `CHECKPOINT REACHED` if absent. |
| T-10-00-02 | Information Disclosure | `TvdbReliabilityDiagnostics` | mitigate | Sanitize diagnostic messages for `Authorization`, bearer tokens, API keys, PINs, and token text before UI/log projection. |
| T-10-00-03 | Repudiation | Diagnostic reason vocabulary | mitigate | Use exact reason codes for provider choice, fallback, update/reference status, stale cache, invalid credentials, date-only gating, poster override, and skipped TMDB TV fetches. |
</threat_model>

<verification>
Run:
`./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.core.tvdb.TvdbReliabilityDiagnosticsTest"`
</verification>

<success_criteria>
Phase 6-9 source bindings are documented, and all later Phase 10 plans can import a typed sanitized diagnostics contract without inventing freeform reason strings.
</success_criteria>

<output>
After completion, create `.planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-00-SUMMARY.md`.
</output>
