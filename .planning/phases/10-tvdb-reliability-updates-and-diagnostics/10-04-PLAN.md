---
phase: 10-tvdb-reliability-updates-and-diagnostics
plan: 04
type: execute
wave: 3
depends_on: ["10-02", "10-03"]
files_modified:
  - app/src/main/java/com/nexio/tv/core/tvdb/TvdbMetadataService.kt
  - app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataRouter.kt
  - app/src/main/java/com/nexio/tv/ui/screens/home/HomeViewModelContinueWatching.kt
  - app/src/main/java/com/nexio/tv/data/repository/ContinueWatchingSnapshotService.kt
  - app/src/test/java/com/nexio/tv/core/tvdb/TvdbGracefulFallbackTest.kt
  - app/src/test/java/com/nexio/tv/core/tvdb/TvdbCredentialHealthTest.kt
autonomous: true
requirements:
  - UX-03
user_setup: []
must_haves:
  truths:
    - "TVDB outages serve last-known-good TVDB data for TV detail and Continue Watching when cached data satisfies the surface."
    - "Invalid credentials block new TVDB calls without purging cached TVDB data."
    - "Missing TVDB fields use field-level fallback reasons while TVDB remains the record provider."
  artifacts:
    - path: "app/src/test/java/com/nexio/tv/core/tvdb/TvdbGracefulFallbackTest.kt"
      provides: "Last-known-good and explicit fallback regression coverage"
      contains: "stale cache served"
    - path: "app/src/test/java/com/nexio/tv/core/tvdb/TvdbCredentialHealthTest.kt"
      provides: "Invalid credential blocking coverage"
      contains: "blocks new tvdb calls"
  key_links:
    - from: "TvMetadataRouter"
      to: "TvdbCredentialHealth"
      via: "network-call gate before TVDB fetch"
      pattern: "canCallTvdb"
    - from: "ContinueWatchingSnapshotService"
      to: "cached TVDB metadata"
      via: "fallback merge preserves display metadata"
      pattern: "STALE_CACHE_SERVED"
---

<objective>
Make TVDB provider failures graceful for TV detail and Continue Watching.

Purpose: Outages or invalid credentials must not make existing TV metadata look randomly blank, late, or inconsistent when safe cached data is available.
Output: last-known-good serving, invalid-credential network gating, field-level fallback reasons, and tests for TV detail/Continue Watching behavior.
</objective>

<execution_context>
@/Users/jneerdael/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jneerdael/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-CONTEXT.md
@.planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-RESEARCH.md
@.planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-VALIDATION.md
@.planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-BINDINGS.md
@app/src/main/java/com/nexio/tv/core/tvdb/TvdbMetadataService.kt
@app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataRouter.kt
@app/src/main/java/com/nexio/tv/core/tvdb/TvdbCredentialHealth.kt
@app/src/main/java/com/nexio/tv/core/tvdb/TvdbReliabilityDiagnostics.kt
@app/src/main/java/com/nexio/tv/data/repository/ContinueWatchingSnapshotService.kt
@app/src/main/java/com/nexio/tv/ui/screens/home/HomeViewModelContinueWatching.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add graceful fallback and credential-health regression tests</name>
  <files>
    app/src/test/java/com/nexio/tv/core/tvdb/TvdbGracefulFallbackTest.kt,
    app/src/test/java/com/nexio/tv/core/tvdb/TvdbCredentialHealthTest.kt
  </files>
  <read_first>
    .planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-BINDINGS.md,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataRouter.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvdbMetadataService.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvdbCredentialHealth.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvdbReliabilityDiagnostics.kt,
    app/src/main/java/com/nexio/tv/data/repository/ContinueWatchingSnapshotService.kt,
    .planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-VALIDATION.md
  </read_first>
  <behavior>
    - A TVDB network outage returns cached TVDB enrichment with `stale_cache_served`.
    - When cached TVDB data cannot satisfy a surface, TMDB fallback is explicit and records `explicit_fallback`.
    - Invalid credentials block new TVDB calls and keep cached data.
    - Missing `airsTime` and date-only gating record field-level reasons without switching record provider away from TVDB.
  </behavior>
  <action>
Create `TvdbGracefulFallbackTest.kt` with tests named exactly. These tests encode D-07, D-08, and D-09: last-known-good data wins during outages, invalid credentials block new TVDB calls without cache purge, and missing fields use field-level fallback reasons:
- `outage serves last known good tv detail before explicit fallback`
- `continue watching outage preserves cached display metadata`
- `missing airs time records field level fallback while tvdb remains provider`
- `date only gating records reason without blanking next up metadata`

Create `TvdbCredentialHealthTest.kt` with tests named exactly:
- `invalid credentials block new tvdb calls without cache purge`
- `valid credentials allow tvdb calls again`
- `invalid credential diagnostic contains no secret material`

Use MockK `coVerify(exactly = 0)` for TVDB API calls after invalid credentials are marked invalid. Assert reason codes `stale_cache_served`, `explicit_fallback`, `missing_airs_time`, `date_only_gating`, and `invalid_credentials` through `TvdbReliabilityReason.code`.
  </action>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.core.tvdb.TvdbGracefulFallbackTest" --tests "com.nexio.tv.core.tvdb.TvdbCredentialHealthTest"</automated>
  </verify>
  <acceptance_criteria>
    - `TvdbGracefulFallbackTest.kt` contains `outage serves last known good tv detail`.
    - `TvdbGracefulFallbackTest.kt` contains `continue watching outage preserves cached display metadata`.
    - `TvdbGracefulFallbackTest.kt` contains `missing_airs_time`.
    - `TvdbGracefulFallbackTest.kt` contains `date_only_gating`.
    - `TvdbCredentialHealthTest.kt` contains `invalid credentials block new tvdb calls without cache purge`.
    - `TvdbCredentialHealthTest.kt` contains `coVerify(exactly = 0)`.
  </acceptance_criteria>
  <done>Regression tests define the graceful failure behavior before provider/router changes are made.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire last-known-good serving and field-level fallback reasons</name>
  <files>
    app/src/main/java/com/nexio/tv/core/tvdb/TvdbMetadataService.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataRouter.kt,
    app/src/main/java/com/nexio/tv/ui/screens/home/HomeViewModelContinueWatching.kt,
    app/src/main/java/com/nexio/tv/data/repository/ContinueWatchingSnapshotService.kt,
    app/src/test/java/com/nexio/tv/core/tvdb/TvdbGracefulFallbackTest.kt,
    app/src/test/java/com/nexio/tv/core/tvdb/TvdbCredentialHealthTest.kt
  </files>
  <read_first>
    .planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-BINDINGS.md,
    app/src/main/java/com/nexio/tv/core/tvdb/TvdbMetadataService.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataRouter.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvdbCredentialHealth.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvdbReliabilityDiagnostics.kt,
    app/src/main/java/com/nexio/tv/data/repository/ContinueWatchingSnapshotService.kt,
    app/src/main/java/com/nexio/tv/ui/screens/home/HomeViewModelContinueWatching.kt
  </read_first>
  <action>
Update `TvdbMetadataService` per D-07 so public TVDB fetch methods first read the existing TVDB disk cache. On network outage, timeout, 5xx, or refresh exception, return a result carrying cached TVDB data plus `TvdbReliabilityReason.STALE_CACHE_SERVED` when cached data satisfies the requested TV detail, episode, or Continue Watching display metadata. Do not return `null` while valid cached data exists.

Update `TvMetadataRouter` per D-08 and D-09 so:
- It checks `TvdbCredentialHealth.canCallTvdb()` before new TVDB network calls.
- If credentials are invalid and cached TVDB data is available, it serves cached TVDB data with `STALE_CACHE_SERVED` and `INVALID_CREDENTIALS`.
- If credentials are invalid and no safe cached data exists, it uses explicit TMDB fallback with `EXPLICIT_FALLBACK` and `INVALID_CREDENTIALS`.
- It records `MISSING_AIRS_TIME` and `DATE_ONLY_GATING` as field-level diagnostics while keeping provider `TVDB` when the core record came from TVDB.
- It records `POSTER_RATINGS_OVERRIDE` when poster-ratings replaces only the poster URL.
- It records `TMDB_TV_FETCH_SKIPPED` on TVDB success where TMDB TV metadata was not fetched.

Update `ContinueWatchingSnapshotService` and `HomeViewModelContinueWatching` only at the existing metadata/fallback merge seams. Preserve existing fallback metadata if TVDB refresh fails, but prefer last-known-good TVDB metadata over blank or TMDB fallback when cached TVDB data satisfies the row. Do not change exact-air-time scheduling logic from Phase 8.
  </action>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.core.tvdb.TvdbGracefulFallbackTest" --tests "com.nexio.tv.core.tvdb.TvdbCredentialHealthTest" --tests "com.nexio.tv.ui.screens.home.HomeViewModelContinueWatchingTest"</automated>
  </verify>
  <acceptance_criteria>
    - `TvMetadataRouter.kt` contains `canCallTvdb`.
    - `TvMetadataRouter.kt` contains `STALE_CACHE_SERVED`.
    - `TvMetadataRouter.kt` contains `EXPLICIT_FALLBACK`.
    - `TvMetadataRouter.kt` contains `MISSING_AIRS_TIME`.
    - `TvMetadataRouter.kt` contains `DATE_ONLY_GATING`.
    - `TvMetadataRouter.kt` contains `TMDB_TV_FETCH_SKIPPED`.
    - `TvdbMetadataService.kt` reads TVDB cache before network fetch paths.
    - No changed file contains `Authorization` or `Bearer` in diagnostic messages.
    - Targeted Gradle test command exits 0.
  </acceptance_criteria>
  <done>TVDB provider failures serve last-known-good metadata first, block invalid-credential network calls, and emit field-level fallback diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TVDB network -> metadata surfaces | Outage/invalid credential states influence TV detail and Continue Watching output. |
| Field-level fallback -> provider diagnostics | Missing fields can be filled from safe sources without changing provider identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Denial of Service | `TvdbMetadataService` / `TvMetadataRouter` | mitigate | Serve last-known-good TVDB data before fallback or empty output during outage/invalid credentials. |
| T-10-04-02 | Information Disclosure | fallback diagnostics | mitigate | Use typed reason codes and sanitized status only; tests assert no secret material in invalid credential diagnostics. |
| T-10-04-03 | Repudiation | fallback branches | mitigate | Emit exact `STALE_CACHE_SERVED`, `EXPLICIT_FALLBACK`, `MISSING_AIRS_TIME`, `DATE_ONLY_GATING`, `POSTER_RATINGS_OVERRIDE`, and `TMDB_TV_FETCH_SKIPPED` reasons. |
</threat_model>

<verification>
Run:
`./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.core.tvdb.TvdbGracefulFallbackTest" --tests "com.nexio.tv.core.tvdb.TvdbCredentialHealthTest" --tests "com.nexio.tv.ui.screens.home.HomeViewModelContinueWatchingTest"`
</verification>

<success_criteria>
TVDB outages and invalid credentials no longer blank safe cached TV detail or Continue Watching metadata, and fallback behavior is explicit and diagnosable.
</success_criteria>

<output>
After completion, create `.planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-04-SUMMARY.md`.
</output>
