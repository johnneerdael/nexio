---
phase: 10-tvdb-reliability-updates-and-diagnostics
plan: 05
type: execute
wave: 5
depends_on: ["10-04"]
files_modified:
  - app/src/main/java/com/nexio/tv/ui/screens/settings/TvdbSettingsViewModel.kt
  - app/src/main/java/com/nexio/tv/ui/screens/settings/TvdbSettingsScreen.kt
  - app/src/main/java/com/nexio/tv/ui/screens/settings/DebugSettingsViewModel.kt
  - app/src/main/java/com/nexio/tv/ui/screens/settings/DebugSettingsScreen.kt
  - app/src/main/res/values/strings.xml
  - app/src/test/java/com/nexio/tv/core/tvdb/TvdbDiagnosticsTest.kt
  - app/src/test/java/com/nexio/tv/ui/screens/settings/TvdbSettingsViewModelTest.kt
autonomous: true
requirements:
  - UX-03
  - CACHE-02
  - CACHE-03
user_setup: []
must_haves:
  truths:
    - "TVDB settings show user-facing status for valid, invalid, stale-cache, and refresh states."
    - "Debug settings expose detailed provider/cache/fallback diagnostics without secrets."
    - "Settings and Debug UI consume the Hilt-bound recorder snapshot created in plan `10-00` without recreating recorder storage or binding."
  artifacts:
    - path: "app/src/main/java/com/nexio/tv/ui/screens/settings/TvdbSettingsViewModel.kt"
      provides: "User-facing TVDB status projection from diagnostics snapshot"
      contains: "tvdbStatusLine"
    - path: "app/src/main/java/com/nexio/tv/ui/screens/settings/DebugSettingsScreen.kt"
      provides: "Debug-visible detailed TVDB diagnostics"
      contains: "tvdb_diagnostics"
  key_links:
    - from: "TvdbDiagnosticsDataStore"
      to: "TvdbSettingsViewModel"
      via: "user status projection"
      pattern: "userStatusLine"
    - from: "TvdbDiagnosticsDataStore"
      to: "DebugSettingsViewModel"
      via: "detailed debug snapshot"
      pattern: "tvdbDiagnostics"
---

<objective>
Surface TVDB reliability diagnostics in settings and Debug UI.

Purpose: Users and maintainers need to understand provider choice, fallback reason, missing exact air time, date-only gating, poster override, skipped TMDB TV fetches, update/reference refresh status, stale cache, and invalid credentials without exposing secrets.
Output: settings/debug UI state, tests, and strings consuming the diagnostics DataStore and recorder implementation created in plan `10-00`.
</objective>

<execution_context>
@/Users/jneerdael/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jneerdael/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-CONTEXT.md
@.planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-RESEARCH.md
@.planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-VALIDATION.md
@.planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-BINDINGS.md
@app/src/main/java/com/nexio/tv/core/tvdb/TvdbReliabilityDiagnostics.kt
@app/src/main/java/com/nexio/tv/ui/screens/settings/TvdbSettingsViewModel.kt
@app/src/main/java/com/nexio/tv/ui/screens/settings/TvdbSettingsScreen.kt
@app/src/main/java/com/nexio/tv/ui/screens/settings/DebugSettingsViewModel.kt
@app/src/main/java/com/nexio/tv/ui/screens/settings/DebugSettingsScreen.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project diagnostics snapshot into settings status and Debug details</name>
  <files>
    app/src/main/java/com/nexio/tv/ui/screens/settings/TvdbSettingsViewModel.kt,
    app/src/main/java/com/nexio/tv/ui/screens/settings/TvdbSettingsScreen.kt,
    app/src/main/java/com/nexio/tv/ui/screens/settings/DebugSettingsViewModel.kt,
    app/src/main/java/com/nexio/tv/ui/screens/settings/DebugSettingsScreen.kt,
    app/src/main/res/values/strings.xml,
    app/src/test/java/com/nexio/tv/core/tvdb/TvdbDiagnosticsTest.kt,
    app/src/test/java/com/nexio/tv/ui/screens/settings/TvdbSettingsViewModelTest.kt
  </files>
  <read_first>
    .planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-BINDINGS.md,
    app/src/main/java/com/nexio/tv/core/tvdb/TvdbReliabilityDiagnostics.kt,
    app/src/main/java/com/nexio/tv/data/local/DebugSettingsDataStore.kt,
    app/src/main/java/com/nexio/tv/ui/screens/settings/TvdbSettingsViewModel.kt,
    app/src/main/java/com/nexio/tv/ui/screens/settings/TvdbSettingsScreen.kt,
    app/src/main/java/com/nexio/tv/ui/screens/settings/DebugSettingsViewModel.kt,
    app/src/main/java/com/nexio/tv/ui/screens/settings/DebugSettingsScreen.kt,
    app/src/main/res/values/strings.xml
  </read_first>
  <behavior>
    - TVDB settings status distinguishes invalid credentials, stale cache served, update refresh failed, and reference refresh failed.
    - Debug settings list detailed reason codes for provider choice, fallback, missing `airsTime`, date-only gating, poster override, skipped TMDB TV fetches, update status, stale cache, and invalid credentials.
    - Settings and Debug UI read only the sanitized `TvdbDiagnosticsSnapshot` exposed by plan `10-00`.
    - This plan does not recreate `TvdbDiagnosticsRecorder` binding or move producer writes out of the shared recorder.
  </behavior>
  <action>
Consume the existing `TvdbDiagnosticsDataStore` and `TvdbDiagnosticsSnapshot` from plan `10-00`; do not recreate `TvdbDiagnosticsRecorder`, add a second recorder implementation, or move the Hilt binding out of `TvdbDiagnosticsModule`. This task implements the settings/debug UI projection portion of D-10 and D-11.

Inject `TvdbDiagnosticsDataStore` into `TvdbSettingsViewModel` and project user-facing status strings from the sanitized snapshot fields created by the recorder. Add UI state fields `tvdbStatusLine: String?` and `tvdbLastRefreshLine: String?`. Use `TvdbReliabilityDiagnostic.userStatusLine()` semantics for status copy: invalid credentials, stale cache, update refresh failed, and reference refresh failed are user-facing; provider choice, poster override, date-only gating, missing `airsTime`, and skipped TMDB TV fetches remain Debug-only.

Update `TvdbSettingsScreen` to show status lines under the existing TVDB settings content. Use string resources:
- `tvdb_status_invalid_credentials`
- `tvdb_status_stale_cache`
- `tvdb_status_update_failed`
- `tvdb_status_reference_failed`

Inject `TvdbDiagnosticsDataStore` into `DebugSettingsViewModel`, add `tvdbDiagnostics: TvdbDiagnosticsSnapshot`, and update `DebugSettingsScreen` with a section key `debug_tvdb_diagnostics`. Show non-secret debug labels for every snapshot field.

Create `TvdbDiagnosticsTest.kt` with tests named:
- `settings projection maps invalid credentials stale cache and refresh failures`
- `debug diagnostics include skipped tmdb and poster override reasons`
- `debug diagnostics include provider fallback date only and missing airs time reasons`
- `settings projection does not expose debug only provider reasons`
- `settings and debug consume existing diagnostics snapshot`

Extend or create `TvdbSettingsViewModelTest.kt` with `settings status shows invalid credentials stale cache and refresh failures`.
  </action>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.core.tvdb.TvdbDiagnosticsTest" --tests "com.nexio.tv.ui.screens.settings.TvdbSettingsViewModelTest"</automated>
  </verify>
  <acceptance_criteria>
    - `TvdbSettingsViewModel.kt` contains `tvdbStatusLine`.
    - `TvdbSettingsViewModel.kt` contains `TvdbDiagnosticsDataStore`.
    - `DebugSettingsViewModel.kt` contains `tvdbDiagnostics`.
    - `DebugSettingsViewModel.kt` contains `TvdbDiagnosticsSnapshot`.
    - `DebugSettingsScreen.kt` contains `debug_tvdb_diagnostics`.
    - `strings.xml` contains `tvdb_status_invalid_credentials`.
    - `TvdbDiagnosticsTest.kt` contains `settings projection maps invalid credentials stale cache and refresh failures`.
    - `TvdbDiagnosticsTest.kt` contains `settings and debug consume existing diagnostics snapshot`.
    - Targeted Gradle test command exits 0.
  </acceptance_criteria>
  <done>TVDB reliability diagnostics from the existing recorder snapshot are visible in TVDB settings and Debug settings without exposing secrets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Diagnostics store -> settings UI | Internal provider/cache/fallback decisions become user-visible text. |
| Diagnostics store -> Debug UI | Internal provider/cache/fallback decisions become maintainer-visible diagnostic detail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | Information Disclosure | settings UI | mitigate | Display sanitized reason/status strings from `TvdbDiagnosticsSnapshot`, not API keys, PINs, bearer tokens, or headers. |
| T-10-05-02 | Repudiation | Debug diagnostics | mitigate | Show exact reason-code-backed fields for provider choice, fallback, missing `airsTime`, date-only gating, poster override, skipped TMDB TV fetches, refresh status, stale cache, and invalid credentials. |
| T-10-05-03 | Information Disclosure | Debug diagnostics | mitigate | Render only snapshot fields from the sanitized recorder created in plan `10-00`; do not expose raw diagnostic messages or request data. |
</threat_model>

<verification>
Run:
`./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.core.tvdb.TvdbDiagnosticsTest" --tests "com.nexio.tv.ui.screens.settings.TvdbSettingsViewModelTest"`
</verification>

<success_criteria>
Users can understand TVDB reliability status in settings, and maintainers can inspect detailed non-secret Debug diagnostics from the shared recorder snapshot created before producers entered the graph.
</success_criteria>

<output>
After completion, create `.planning/phases/10-tvdb-reliability-updates-and-diagnostics/10-05-SUMMARY.md`.
</output>
