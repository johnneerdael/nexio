---
phase: 05-nexio-web-integration
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/test/java/com/nexio/tv/sync/ProfileSyncServiceTest.kt
  - app/src/test/java/com/nexio/tv/sync/ProfileAuthSyncTest.kt
  - app/src/test/java/com/nexio/tv/sync/ProfileCatalogSyncTest.kt
  - app/src/test/java/com/nexio/tv/sync/ProfileFormatterSyncTest.kt
  - app/src/test/java/com/nexio/tv/profile/ProfileAvatarTest.kt
autonomous: true
requirements:
  - WEB-01
  - WEB-02
  - WEB-03
  - WEB-04
  - WEB-05
must_haves:
  truths:
    - "Every WEB requirement has a concrete Gradle test file before Phase 5 implementation begins"
    - "Validation commands in downstream plans can reference real test classes instead of structural-only checks"
    - "The Wave 0 test files document the expected behavior for web-created profiles, auth tokens, settings blobs, and avatar URLs"
  artifacts:
    - path: "app/src/test/java/com/nexio/tv/sync/ProfileSyncServiceTest.kt"
      provides: "WEB-01 profile CRUD sync contract test scaffold"
      contains: "WEB-01"
    - path: "app/src/test/java/com/nexio/tv/sync/ProfileAuthSyncTest.kt"
      provides: "WEB-02 per-profile auth token sync contract test scaffold"
      contains: "WEB-02"
    - path: "app/src/test/java/com/nexio/tv/sync/ProfileCatalogSyncTest.kt"
      provides: "WEB-03 catalog ordering sync contract test scaffold"
      contains: "WEB-03"
    - path: "app/src/test/java/com/nexio/tv/sync/ProfileFormatterSyncTest.kt"
      provides: "WEB-04 formatter settings sync contract test scaffold"
      contains: "WEB-04"
    - path: "app/src/test/java/com/nexio/tv/profile/ProfileAvatarTest.kt"
      provides: "WEB-05 avatar URL rendering/cache contract test scaffold"
      contains: "WEB-05"
  key_links:
    - from: "05-VALIDATION.md"
      to: "app/src/test/java/com/nexio/tv/sync/ProfileSyncServiceTest.kt"
      via: "Wave 0 test file required by validation map"
      pattern: "ProfileSyncServiceTest"
    - from: "05-VALIDATION.md"
      to: "app/src/test/java/com/nexio/tv/profile/ProfileAvatarTest.kt"
      via: "Wave 0 test file required by validation map"
      pattern: "ProfileAvatarTest"
user_setup: []
---

<objective>
Create the Phase 5 Wave 0 validation test scaffolds required by 05-VALIDATION.md for WEB-01 through WEB-05.

Purpose: Downstream Phase 5 work must have executable validation anchors before implementation. These are intentionally focused contract scaffolds that compile and can be expanded by the feature plans.
Output: Five Android unit test files mapped one-to-one to WEB-01..WEB-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-nexio-web-integration/05-VALIDATION.md
@.planning/phases/05-nexio-web-integration/05-CONTEXT.md

<interfaces>
From 05-VALIDATION.md:
```text
WEB-01 -> com.nexio.tv.sync.ProfileSyncServiceTest
WEB-02 -> com.nexio.tv.sync.ProfileAuthSyncTest
WEB-03 -> com.nexio.tv.sync.ProfileCatalogSyncTest
WEB-04 -> com.nexio.tv.sync.ProfileFormatterSyncTest
WEB-05 -> com.nexio.tv.profile.ProfileAvatarTest
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WEB-01 and WEB-02 sync contract test scaffolds</name>
  <files>
    app/src/test/java/com/nexio/tv/sync/ProfileSyncServiceTest.kt,
    app/src/test/java/com/nexio/tv/sync/ProfileAuthSyncTest.kt
  </files>
  <read_first>
    .planning/phases/05-nexio-web-integration/05-VALIDATION.md,
    app/src/test/java/com/nexio/tv/core/sync/ProfileSettingsSyncServiceTest.kt,
    app/src/main/java/com/nexio/tv/core/sync/ProfileSyncService.kt,
    app/src/main/java/com/nexio/tv/core/sync/ProfileSettingsSyncService.kt
  </read_first>
  <action>
Create package `com.nexio.tv.sync` test files for WEB-01 and WEB-02. Follow the repository's existing JVM/Robolectric test style from `ProfileSettingsSyncServiceTest.kt`; if that test uses a local `typealias AndroidJUnit4 = RobolectricTestRunner`, copy that pattern so the test source set compiles without adding dependencies.

`ProfileSyncServiceTest.kt` must contain a test named `WEB_01_profile_crud_sync_contract_is_documented` that asserts the expected contract strings for profile CRUD sync are present in a local list: `profile_upsert`, `profile_delete`, `sync_pull_profiles`, and `profile_index`. This scaffold is the anchor that later plans expand with real service assertions.

`ProfileAuthSyncTest.kt` must contain a test named `WEB_02_profile_auth_sync_contract_is_documented` that asserts these contract strings: `profile_auth_tokens`, `trakt_access_token`, `trakt_refresh_token`, `simkl_access_token`, and `updated_at`. Also include a second test named `WEB_02_token_payload_allows_json_values` that uses `kotlinx.serialization.json.JsonObject` / `JsonElement` for a sample payload containing string and numeric values. This explicitly prevents the Map<String, String> token payload bug.
  </action>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.sync.ProfileSyncServiceTest" --tests "com.nexio.tv.sync.ProfileAuthSyncTest" -x lint</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/test/java/com/nexio/tv/sync/ProfileSyncServiceTest.kt` exists and contains `WEB-01`
    - `ProfileSyncServiceTest.kt` contains `profile_upsert`, `profile_delete`, and `sync_pull_profiles`
    - `app/src/test/java/com/nexio/tv/sync/ProfileAuthSyncTest.kt` exists and contains `WEB-02`
    - `ProfileAuthSyncTest.kt` contains `profile_auth_tokens`, `trakt_access_token`, `trakt_refresh_token`, and `simkl_access_token`
    - `ProfileAuthSyncTest.kt` uses `JsonElement` or `JsonObject` for token payload values
    - Targeted Gradle test command exits 0, or any failure is unrelated to these new files and clearly reported
  </acceptance_criteria>
  <done>WEB-01 and WEB-02 test scaffold files exist, compile in the unit-test source set, and encode the required data contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Create WEB-03 and WEB-04 settings sync contract test scaffolds</name>
  <files>
    app/src/test/java/com/nexio/tv/sync/ProfileCatalogSyncTest.kt,
    app/src/test/java/com/nexio/tv/sync/ProfileFormatterSyncTest.kt
  </files>
  <read_first>
    .planning/phases/05-nexio-web-integration/05-VALIDATION.md,
    .planning/phases/04-sync-and-cleanup/04-02-SUMMARY.md,
    app/src/main/java/com/nexio/tv/core/sync/ProfileSettingsSyncService.kt,
    app/src/main/java/com/nexio/tv/data/remote/supabase/AccountSyncModels.kt
  </read_first>
  <action>
Create `ProfileCatalogSyncTest.kt` and `ProfileFormatterSyncTest.kt` in package `com.nexio.tv.sync`.

`ProfileCatalogSyncTest.kt` must contain a test named `WEB_03_catalog_settings_use_phase4_blob_rpcs` that asserts the resolved Phase 4 RPC names are `sync_pull_profile_settings_blob` and `sync_push_profile_settings_blob`, and asserts the settings blob path names `catalogs`, `homeCatalogOrderKeys`, and `disabledHomeCatalogKeys`.

`ProfileFormatterSyncTest.kt` must contain a test named `WEB_04_formatter_settings_use_phase4_blob_rpcs` that asserts the same resolved RPC names and the formatter keys `formatter`, `selectedTemplateId`, `customTemplate`, and `enabled`.
  </action>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.sync.ProfileCatalogSyncTest" --tests "com.nexio.tv.sync.ProfileFormatterSyncTest" -x lint</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/test/java/com/nexio/tv/sync/ProfileCatalogSyncTest.kt` exists and contains `WEB-03`
    - `ProfileCatalogSyncTest.kt` contains `sync_pull_profile_settings_blob` and `sync_push_profile_settings_blob`
    - `ProfileCatalogSyncTest.kt` contains `homeCatalogOrderKeys` and `disabledHomeCatalogKeys`
    - `app/src/test/java/com/nexio/tv/sync/ProfileFormatterSyncTest.kt` exists and contains `WEB-04`
    - `ProfileFormatterSyncTest.kt` contains `sync_pull_profile_settings_blob` and `sync_push_profile_settings_blob`
    - `ProfileFormatterSyncTest.kt` contains `selectedTemplateId`, `customTemplate`, and `enabled`
    - Targeted Gradle test command exits 0, or any failure is unrelated to these new files and clearly reported
  </acceptance_criteria>
  <done>WEB-03 and WEB-04 test scaffold files exist and anchor catalog/formatter validation to the resolved Phase 4 blob RPC names.</done>
</task>

<task type="auto">
  <name>Task 3: Create WEB-05 avatar contract test scaffold</name>
  <files>
    app/src/test/java/com/nexio/tv/profile/ProfileAvatarTest.kt
  </files>
  <read_first>
    .planning/phases/05-nexio-web-integration/05-VALIDATION.md,
    app/src/main/java/com/nexio/tv/domain/model/UserProfile.kt,
    app/src/main/java/com/nexio/tv/ui/components/ProfileAvatarCircle.kt,
    app/src/main/java/com/nexio/tv/data/remote/supabase/SupabaseModels.kt
  </read_first>
  <action>
Create `ProfileAvatarTest.kt` in package `com.nexio.tv.profile`.

The file must contain a test named `WEB_05_avatar_contract_uses_url_with_color_fallback` that asserts these contract strings are present: `avatar_url`, `avatarUrl`, `avatarColorHex`, `profile-avatars`, and `?t=`. Include a second test named `WEB_05_avatar_url_field_is_nullable_contract` that documents the expected nullable field by asserting a local `String?` can be null. This is a scaffold for the later TV avatar rendering plan.
  </action>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.profile.ProfileAvatarTest" -x lint</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/test/java/com/nexio/tv/profile/ProfileAvatarTest.kt` exists and contains `WEB-05`
    - `ProfileAvatarTest.kt` contains `avatar_url`, `avatarUrl`, `avatarColorHex`, `profile-avatars`, and `?t=`
    - `ProfileAvatarTest.kt` contains a nullable `String?` avatar URL contract assertion
    - Targeted Gradle test command exits 0, or any failure is unrelated to this new file and clearly reported
  </acceptance_criteria>
  <done>WEB-05 avatar validation scaffold exists and documents URL-based invalidation plus color fallback requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Validation scaffold -> implementation plans | Tests must not encode false contracts that later plans implement incorrectly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-00-01 | Tampering | Test contract names | mitigate | Test scaffolds assert resolved RPC names and token payload JSON shape from Phase 4 artifacts and locked Phase 5 decisions |
| T-05-00-02 | Information Disclosure | Token payload test fixture | mitigate | Tests use synthetic strings only; no real tokens or secrets appear in fixtures |
</threat_model>

<verification>
1. `./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.sync.ProfileSyncServiceTest" --tests "com.nexio.tv.sync.ProfileAuthSyncTest" -x lint`
2. `./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.sync.ProfileCatalogSyncTest" --tests "com.nexio.tv.sync.ProfileFormatterSyncTest" -x lint`
3. `./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.profile.ProfileAvatarTest" -x lint`
</verification>

<success_criteria>
- All five Wave 0 test files listed in 05-VALIDATION.md exist
- Each WEB requirement WEB-01..WEB-05 maps to at least one test scaffold
- Token payload scaffold uses JSON element/object values, not `Map<String, String>`
- Catalog/formatter scaffolds use `sync_push_profile_settings_blob` and `sync_pull_profile_settings_blob`
</success_criteria>

<output>
After completion, create `.planning/phases/05-nexio-web-integration/05-00-SUMMARY.md`
</output>
