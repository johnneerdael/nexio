---
phase: 09-tvdb-advanced-tv-surfaces
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataModels.kt
  - app/src/main/java/com/nexio/tv/core/tvdb/TvdbAdvancedMetadataMapper.kt
  - app/src/main/java/com/nexio/tv/core/tvdb/TvdbMetadataService.kt
  - app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataDiagnostics.kt
  - app/src/main/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModel.kt
  - app/src/main/java/com/nexio/tv/domain/model/HomeDisplayMetadata.kt
  - app/src/main/java/com/nexio/tv/ui/screens/home/HomeViewModelPresentationPipeline.kt
  - app/src/main/java/com/nexio/tv/ui/screens/stream/StreamScreenViewModel.kt
  - app/src/main/java/com/nexio/tv/data/repository/IdleScreensaverPreparation.kt
  - app/src/main/java/com/nexio/tv/ui/screens/player/PlayerRuntimeControllerMetadata.kt
  - app/src/test/java/com/nexio/tv/core/tvdb/TvdbAdvancedMetadataMapperTest.kt
  - app/src/test/java/com/nexio/tv/core/tvdb/TvdbProviderRoutingTest.kt
  - app/src/test/java/com/nexio/tv/ui/screens/detail/MetaDetailsTvdbAdvancedMetadataTest.kt
  - app/src/test/java/com/nexio/tv/ui/shared/MetaSharedTvdbSurfacePropagationTest.kt
autonomous: true
requirements:
  - META-05
user_setup: []
must_haves:
  truths:
    - "TVDB characters/cast populate existing cast surfaces with names, character names, and photos"
    - "TVDB companies and networks populate existing `MetaCompany` surfaces with kind preserved"
    - "TVDB genres and content ratings replace TMDB TV equivalents when TVDB is active"
    - "TVDB advanced metadata reaches existing Home, stream, screensaver, and player metadata surfaces through shared `Meta`/`HomeDisplayMetadata` propagation without adding new UI"
    - "TVDB active advanced metadata success skips duplicate TMDB TV metadata calls and records diagnostics"
  artifacts:
    - path: "app/src/main/java/com/nexio/tv/core/tvdb/TvdbAdvancedMetadataMapper.kt"
      provides: "Mapping from TVDB advanced series fields into existing domain metadata contracts"
      contains: "class TvdbAdvancedMetadataMapper"
    - path: "app/src/main/java/com/nexio/tv/core/tvdb/TvdbMetadataService.kt"
      provides: "TVDB advanced metadata included in provider enrichment"
      contains: "TvdbAdvancedMetadataMapper"
    - path: "app/src/main/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModel.kt"
      provides: "Existing detail cast/company/network/genre/rating surfaces populated from TVDB router output"
      contains: "castMembers ="
    - path: "app/src/main/java/com/nexio/tv/domain/model/HomeDisplayMetadata.kt"
      provides: "Shared Home/continue-watching/screensaver metadata projection from hydrated `Meta`"
      contains: "fun Meta.toHomeDisplayMetadata"
    - path: "app/src/main/java/com/nexio/tv/ui/screens/home/HomeViewModelPresentationPipeline.kt"
      provides: "Focused Home preview enrichment from hydrated `Meta`"
      contains: "externalMeta.genres"
    - path: "app/src/main/java/com/nexio/tv/ui/screens/stream/StreamScreenViewModel.kt"
      provides: "Stream screen metadata hydration from `Meta`"
      contains: "val metaGenres = meta.genres"
    - path: "app/src/main/java/com/nexio/tv/data/repository/IdleScreensaverPreparation.kt"
      provides: "Screensaver slide metadata hydration from `Meta.toHomeDisplayMetadata`"
      contains: "externalMeta?.toHomeDisplayMetadata()"
  key_links:
    - from: "TvdbMetadataService"
      to: "TvdbAdvancedMetadataMapper"
      via: "SeriesExtendedRecord characters/companies/networks/genres/contentRatings mapping"
      pattern: "mapAdvancedMetadata"
    - from: "MetaDetailsViewModel"
      to: "TvMetadataRouter.fetchEnrichment"
      via: "existing metadata toggle groups, no new UI sections"
      pattern: "tvMetadataRouter.fetchEnrichment"
    - from: "TvdbMetadataService"
      to: "Home focused previews"
      via: "`Meta` -> `HomeViewModelPresentationPipeline.mergeFocusedItemEnrichment` -> `MetaPreview`"
      pattern: "externalMeta\\.genres|externalMeta\\.trailerYtIds"
    - from: "TvdbMetadataService"
      to: "Stream screen metadata"
      via: "`MetaRepository.getMetaFromAllAddons` hydrated `Meta` -> `StreamScreenViewModel`"
      pattern: "meta\\.genres.*state\\.genres"
    - from: "TvdbMetadataService"
      to: "Screensaver slides"
      via: "`Meta` -> `HomeDisplayMetadata` -> `IdleScreensaverSlide`"
      pattern: "externalMeta\\?\\.toHomeDisplayMetadata\\(\\).*genres = preview\\.genres"
---

<objective>
Replace existing TMDB TV advanced metadata surfaces with TVDB data.

Purpose: Phase 9 must capture the TV-specific TVDB surfaces that Phase 7 deferred while preserving existing UI contracts and avoiding new visible metadata sections.
Output: TVDB advanced mapper, provider service/router integration, detail surface wiring, diagnostics, and tests.
</objective>

<execution_context>
@/Users/jneerdael/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jneerdael/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-tvdb-advanced-tv-surfaces/09-CONTEXT.md
@.planning/phases/09-tvdb-advanced-tv-surfaces/09-RESEARCH.md
@.planning/phases/09-tvdb-advanced-tv-surfaces/09-VALIDATION.md
@.planning/phases/09-tvdb-advanced-tv-surfaces/09-00-SUMMARY.md
@.planning/phases/09-tvdb-advanced-tv-surfaces/09-01-SUMMARY.md
@tvdb.yml
@app/src/main/java/com/nexio/tv/domain/model/Meta.kt
@app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataModels.kt
@app/src/main/java/com/nexio/tv/core/tvdb/TvdbMetadataService.kt
@app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataRouter.kt
@app/src/main/java/com/nexio/tv/core/tmdb/TmdbMetadataService.kt
@app/src/main/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModel.kt
@app/src/main/java/com/nexio/tv/ui/screens/detail/MetaDetailsScreen.kt
@app/src/main/java/com/nexio/tv/domain/model/HomeDisplayMetadata.kt
@app/src/main/java/com/nexio/tv/ui/screens/home/HomeViewModelPresentationPipeline.kt
@app/src/main/java/com/nexio/tv/ui/screens/stream/StreamScreenViewModel.kt
@app/src/main/java/com/nexio/tv/data/repository/IdleScreensaverPreparation.kt
@app/src/main/java/com/nexio/tv/ui/screens/player/PlayerRuntimeControllerMetadata.kt

<interfaces>
Existing user-facing contracts to populate, not replace:
```kotlin
data class MetaCastMember(
    val name: String,
    val character: String? = null,
    val photo: String? = null,
    val tmdbId: Int? = null
)

data class MetaCompany(
    val tmdbId: Int? = null,
    val name: String,
    val logo: String? = null,
    val kind: MetaCompanyKind = MetaCompanyKind.COMPANY
)

enum class MetaCompanyKind { COMPANY, NETWORK }
```

Existing detail toggle groups in `MetaDetailsViewModel.enrichMeta`:
```text
settings.useBasicInfo -> name, description, genres, rating
settings.useCredits -> cast/director/writer/castMembers
settings.useProductions -> productionCompanies
settings.useNetworks -> networks
settings.useDetails -> runtime, releaseInfo, ageRating, country, language
```

Existing non-detail shared propagation paths for D-12:
```text
Home: Meta -> HomeViewModelPresentationPipeline.mergeFocusedItemEnrichment(externalMeta) -> MetaPreview.genres/trailerYtIds/language/description/rating
Home/continue-watching display metadata: Meta -> HomeDisplayMetadata.toHomeDisplayMetadata() -> HomeDisplayMetadata.applyTo(MetaPreview)
Stream screen: MetaRepository.getMetaFromAllAddons(...) -> StreamScreenViewModel hydrated Meta -> state.genres/year/runtime/poster/backdrop/logo
Screensaver: fetchIdleScreensaverMeta(...) -> mergeHydratedScreensaverMetadata(...) -> HomeDisplayMetadata.applyTo(...) -> IdleScreensaverSlide.genres/description/runtime/rating
Player/runtime overlay: PlayerRuntimeController.applyMetaDetails(Meta) -> castMembers/description
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map TVDB advanced fields into existing domain surfaces</name>
  <files>
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataModels.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvdbAdvancedMetadataMapper.kt,
    app/src/test/java/com/nexio/tv/core/tvdb/TvdbAdvancedMetadataMapperTest.kt
  </files>
  <read_first>
    app/src/main/java/com/nexio/tv/domain/model/Meta.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataModels.kt,
    app/src/main/java/com/nexio/tv/data/remote/api/TvdbApi.kt,
    app/src/main/java/com/nexio/tv/core/tmdb/TmdbMetadataService.kt,
    app/src/test/java/com/nexio/tv/core/tvdb/TvdbAdvancedMetadataMapperTest.kt,
    .planning/phases/09-tvdb-advanced-tv-surfaces/09-CONTEXT.md,
    tvdb.yml
  </read_first>
  <behavior>
    - TVDB characters map to `MetaCastMember`
    - TVDB original/latest networks map to `MetaCompany(kind = NETWORK)`
    - TVDB companies map to `MetaCompany(kind = COMPANY)` and do not duplicate network names
    - TVDB genres and content ratings replace TMDB TV equivalents when non-empty
    - Nullable/blank remote metadata is safely omitted
  </behavior>
  <action>
Extend `TvMetadataEnrichment` in `TvMetadataModels.kt` with these defaulted fields if Phase 7 did not already add them:
```kotlin
val directorMembers: List<MetaCastMember> = emptyList()
val writerMembers: List<MetaCastMember> = emptyList()
val castMembers: List<MetaCastMember> = emptyList()
val director: List<String> = emptyList()
val writer: List<String> = emptyList()
val productionCompanies: List<MetaCompany> = emptyList()
val networks: List<MetaCompany> = emptyList()
```

Create `TvdbAdvancedMetadataMapper.kt` with `class TvdbAdvancedMetadataMapper @Inject constructor(private val localeResolver: AppLocaleResolver)` or use the existing Phase 7 language preference source if `AppLocaleResolver` is not available in this package.

Add:
```kotlin
fun mapAdvancedMetadata(series: TvdbSeriesExtendedRecord, preferredCountryCodes: List<String>): TvdbAdvancedMetadata
```

`TvdbAdvancedMetadata` may be an internal data class or direct helper result, but it must expose `castMembers`, `productionCompanies`, `networks`, `genres`, and `ageRating`.

Use these concrete mapping rules per D-09, D-10, and D-11:
- Character/person name source order: `personName`, `personNameTranslations[preferred language]`, then null. Blank names are omitted.
- Character role source: `name` trimmed, null if blank.
- Character photo source: `personImgURL` trimmed, null if blank.
- Character sort: ascending `sort`, null sort last.
- `MetaCastMember.tmdbId` remains null for TVDB characters.
- Networks source: `originalNetwork` then `latestNetwork`, distinct by case-insensitive name, `kind = MetaCompanyKind.NETWORK`.
- Production companies source: `companies`, distinct by case-insensitive name, `kind = MetaCompanyKind.COMPANY`, exclude names already in networks.
- Genres source: nonblank `genre.name`, distinct preserving TVDB order.
- Content rating source: choose first matching preferred country code from `preferredCountryCodes`, then `US`, then first nonblank rating. Return display value as the TVDB rating string only, e.g. `TV-14`.

Do not add a new cast/company/network UI section per D-12 and do not fetch `/companies/types`; Phase 10 owns heavy reference-data caching.
  </action>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.core.tvdb.TvdbAdvancedMetadataMapperTest"</automated>
  </verify>
  <acceptance_criteria>
    - `TvMetadataModels.kt` contains `castMembers: List<MetaCastMember>`
    - `TvMetadataModels.kt` contains `productionCompanies: List<MetaCompany>`
    - `TvMetadataModels.kt` contains `networks: List<MetaCompany>`
    - `TvdbAdvancedMetadataMapper.kt` contains `class TvdbAdvancedMetadataMapper`
    - `TvdbAdvancedMetadataMapper.kt` contains `fun mapAdvancedMetadata(`
    - `TvdbAdvancedMetadataMapper.kt` contains `MetaCompanyKind.NETWORK`
    - `TvdbAdvancedMetadataMapper.kt` contains `MetaCompanyKind.COMPANY`
    - `TvdbAdvancedMetadataMapper.kt` contains `preferredCountryCodes`
    - `TvdbAdvancedMetadataMapperTest.kt` contains `maps characters companies networks genres and content ratings`
    - Targeted Gradle test command exits 0
  </acceptance_criteria>
  <done>TVDB advanced series fields map into the existing domain metadata surfaces without new UI sections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Include advanced TVDB surfaces in provider service and diagnostics</name>
  <files>
    app/src/main/java/com/nexio/tv/core/tvdb/TvdbMetadataService.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataDiagnostics.kt,
    app/src/test/java/com/nexio/tv/core/tvdb/TvdbAdvancedMetadataMapperTest.kt,
    app/src/test/java/com/nexio/tv/core/tvdb/TvdbProviderRoutingTest.kt
  </files>
  <read_first>
    app/src/main/java/com/nexio/tv/core/tvdb/TvdbMetadataService.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvdbAdvancedMetadataMapper.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataDiagnostics.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataRouter.kt,
    app/src/main/java/com/nexio/tv/core/tmdb/TmdbMetadataService.kt,
    app/src/test/java/com/nexio/tv/core/tvdb/TvdbProviderRoutingTest.kt,
    tvdb.yml
  </read_first>
  <behavior>
    - `TvdbMetadataService.fetchSeriesEnrichment` includes advanced mapper output
    - TVDB advanced success emits or carries `tvdb_advanced_surface_success`
    - Missing TVDB advanced data emits or carries `tvdb_advanced_surface_missing`
    - TVDB active advanced success emits or carries `tmdb_tv_skipped`
    - TMDB TV fallback remains explicit and tested
  </behavior>
  <action>
Inject `TvdbAdvancedMetadataMapper` into `TvdbMetadataService`.

When building `TvMetadataEnrichment` from `TvdbSeriesExtendedRecord`, call `advancedMetadataMapper.mapAdvancedMetadata(seriesRecord, preferredCountryCodes)` and set:
- `castMembers`
- `productionCompanies`
- `networks`
- `genres` from TVDB advanced mapper when non-empty
- `ageRating` from TVDB advanced mapper when non-null

In `TvMetadataDiagnostics.kt`, add enum values with exact event strings:
- `TVDB_ADVANCED_SURFACE_SUCCESS("tvdb_advanced_surface_success")`
- `TVDB_ADVANCED_SURFACE_MISSING("tvdb_advanced_surface_missing")`

In `TvdbMetadataService` or `TvMetadataRouter`, add diagnostics/logs:
- `tvdb_advanced_surface_success` when at least one of cast, production companies, networks, genres, or age rating is supplied by TVDB.
- `tvdb_advanced_surface_missing` when TVDB series enrichment succeeds but all advanced surfaces are empty/null.
- `tmdb_tv_skipped` when TVDB advanced surfaces succeed and no TMDB TV enrichment call is made.
- `tvdb_fallback_tmdb` only in explicit fallback branches when TVDB is inactive, invalid, unavailable, or lacks a required record.

Do not merge TVDB and TMDB advanced fields in normal TV success paths per D-11. If TVDB advanced metadata succeeds with non-empty genres or age rating, those values replace TMDB TV genres/content ratings for the TV surface.
  </action>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.core.tvdb.TvdbAdvancedMetadataMapperTest" --tests "com.nexio.tv.core.tvdb.TvdbProviderRoutingTest"</automated>
  </verify>
  <acceptance_criteria>
    - `TvdbMetadataService.kt` contains `TvdbAdvancedMetadataMapper`
    - `TvdbMetadataService.kt` contains `mapAdvancedMetadata`
    - `TvdbMetadataService.kt` contains `castMembers =`
    - `TvdbMetadataService.kt` contains `productionCompanies =`
    - `TvdbMetadataService.kt` contains `networks =`
    - `TvMetadataDiagnostics.kt` contains `TVDB_ADVANCED_SURFACE_SUCCESS("tvdb_advanced_surface_success")`
    - `TvMetadataDiagnostics.kt` contains `TVDB_ADVANCED_SURFACE_MISSING("tvdb_advanced_surface_missing")`
    - `TvdbProviderRoutingTest.kt` contains `advanced tvdb success records skipped tmdb tv call`
    - `TvdbProviderRoutingTest.kt` contains `coVerify(exactly = 0)`
    - Targeted Gradle test command exits 0
  </acceptance_criteria>
  <done>TVDB provider output carries advanced TV surfaces and observable provider-precedence diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apply TVDB advanced surfaces to existing metadata roles</name>
  <files>
    app/src/main/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModel.kt,
    app/src/main/java/com/nexio/tv/domain/model/HomeDisplayMetadata.kt,
    app/src/main/java/com/nexio/tv/ui/screens/home/HomeViewModelPresentationPipeline.kt,
    app/src/main/java/com/nexio/tv/ui/screens/stream/StreamScreenViewModel.kt,
    app/src/main/java/com/nexio/tv/data/repository/IdleScreensaverPreparation.kt,
    app/src/main/java/com/nexio/tv/ui/screens/player/PlayerRuntimeControllerMetadata.kt,
    app/src/test/java/com/nexio/tv/ui/screens/detail/MetaDetailsTvdbAdvancedMetadataTest.kt,
    app/src/test/java/com/nexio/tv/ui/shared/MetaSharedTvdbSurfacePropagationTest.kt
  </files>
  <read_first>
    app/src/main/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModel.kt,
    app/src/main/java/com/nexio/tv/ui/screens/detail/MetaDetailsScreen.kt,
    app/src/main/java/com/nexio/tv/domain/model/HomeDisplayMetadata.kt,
    app/src/main/java/com/nexio/tv/ui/screens/home/HomeViewModelPresentationPipeline.kt,
    app/src/main/java/com/nexio/tv/ui/screens/home/ModernHomeModels.kt,
    app/src/main/java/com/nexio/tv/ui/screens/stream/StreamScreenViewModel.kt,
    app/src/main/java/com/nexio/tv/data/repository/IdleScreensaverPreparation.kt,
    app/src/main/java/com/nexio/tv/ui/screens/player/PlayerRuntimeControllerMetadata.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataRouter.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataModels.kt,
    app/src/main/java/com/nexio/tv/domain/model/Meta.kt,
    app/src/test/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModelTestFactory.kt,
    app/src/test/java/com/nexio/tv/domain/model/HomeDisplayMetadataTest.kt,
    app/src/test/java/com/nexio/tv/ui/screens/home/HomeViewModelPresentationPipelineTest.kt,
    app/src/test/java/com/nexio/tv/data/repository/IdleScreensaverRepositoryTest.kt,
    .planning/phases/09-tvdb-advanced-tv-surfaces/09-CONTEXT.md
  </read_first>
  <behavior>
    - Existing detail cast surface receives TVDB cast when `settings.useCredits` is true
    - Existing detail production company surface receives TVDB companies when `settings.useProductions` is true
    - Existing detail network surface receives TVDB networks when `settings.useNetworks` is true
    - Existing detail genres and age rating receive TVDB values through current toggle groups
    - Existing Home focused preview and Home/continue-watching display metadata receive TVDB `Meta` values through `HomeDisplayMetadata` and `mergeFocusedItemEnrichment`
    - Existing stream screen metadata receives TVDB `Meta.genres`, runtime, year, and artwork through its hydrated-meta path
    - Existing screensaver slides receive TVDB `Meta` values through `mergeHydratedScreensaverMetadata`
    - Existing player/runtime metadata receives TVDB `Meta.castMembers` and descriptions through `applyMetaDetails`
    - No new user-visible metadata sections are added
  </behavior>
  <action>
Update the TVDB branch of `MetaDetailsViewModel.enrichMeta` introduced by Phase 7 so `TvMetadataEnrichment` applies the same advanced fields currently applied from `TmdbEnrichment`:
- Under `settings.useBasicInfo`, replace `genres` with `enrichment.genres` when non-empty and `imdbRating` with `enrichment.rating`.
- Under `settings.useDetails`, replace `ageRating`, `country`, and `language` with TVDB enrichment values when present.
- Under `settings.useCredits`, set `castMembers = enrichment.castMembers` when non-empty and set `cast = enrichment.castMembers.map { it.name }` when non-empty. Preserve existing director/writer behavior only if TVDB supplies `directorMembers` or `writerMembers`; do not synthesize creators from companies.
- Under `settings.useProductions`, set `productionCompanies = enrichment.productionCompanies` when non-empty.
- Under `settings.useNetworks`, set `networks = enrichment.networks` when non-empty.

For D-12, prove or add the existing non-detail propagation paths rather than creating new UI:
- Home focused preview must receive hydrated TVDB `Meta` through `HomeViewModelPresentationPipeline.mergeFocusedItemEnrichment(externalMeta)` and carry at least `genres`, `description`, `imdbRating`, `trailerYtIds`, and `language` into `MetaPreview`.
- Home/continue-watching display metadata and screensaver hydration must use `Meta.toHomeDisplayMetadata()` so hydrated TVDB `Meta.genres`, description, rating, runtime, and artwork flow through `HomeDisplayMetadata.applyTo(...)`.
- Stream screen metadata must keep its hydrated-meta path from `MetaRepository.getMetaFromAllAddons(...)` to `StreamScreenViewModel` and update `state.genres`, `year`, `runtime`, `poster`, `backdrop`, and `logo` from the returned `Meta`.
- Player/runtime metadata must keep `PlayerRuntimeController.applyMetaDetails(meta)` reading `Meta.castMembers` so TVDB cast can appear where the runtime overlay already exposes cast metadata.

Do not add any Compose sections to `MetaDetailsScreen.kt`, `CastSection.kt`, Home, stream, screensaver, or player UI per D-12. Existing surfaces must render the new data through shared `Meta` and `HomeDisplayMetadata` propagation. If any static assertion below shows the shared path is absent after Phase 7/9 changes, add the smallest source change in the named propagation file to restore that path; do not add provider-specific UI.

Add `MetaDetailsTvdbAdvancedMetadataTest.kt` with tests named:
- `detail uses tvdb cast companies networks genres and age rating`
- `detail does not add new tvdb metadata section`

The second test must be a source/static assertion that `MetaDetailsScreen.kt` does not contain `TVDB Info`, `Season Type`, `TheTVDB section`, or `TvdbAdvanced`.

Add `MetaSharedTvdbSurfacePropagationTest.kt` with source/static assertion tests named:
- `home shared metadata path carries tvdb meta into existing preview fields`
- `stream screen shared metadata path reads hydrated meta genres runtime and artwork`
- `screensaver shared metadata path carries tvdb meta into slides`
- `player runtime shared metadata path reads tvdb cast members from meta`

Those tests must assert these concrete source links:
- `HomeDisplayMetadata.kt` contains `fun Meta.toHomeDisplayMetadata()` plus assignments for `genres = genres`, `description = description`, `runtime = runtime`, and `imdbRating = imdbRating`.
- `HomeViewModelPresentationPipeline.kt` contains `externalMeta.genres`, `externalMeta.description`, `externalMeta.imdbRating`, `externalMeta.trailerYtIds`, and `externalMeta.language`.
- `StreamScreenViewModel.kt` contains `val metaGenres = meta.genres`, `extractRuntimeMinutes(meta)`, and `genres = genresValue`.
- `IdleScreensaverPreparation.kt` contains `externalMeta?.toHomeDisplayMetadata()` and `genres = preview.genres`.
- `PlayerRuntimeControllerMetadata.kt` contains `sanitizeCastMembers(meta)` and assigns `_uiState` `castMembers` from `safeCastMembers`.
  </action>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.ui.screens.detail.MetaDetailsTvdbAdvancedMetadataTest" --tests "com.nexio.tv.ui.shared.MetaSharedTvdbSurfacePropagationTest"</automated>
  </verify>
  <acceptance_criteria>
    - `MetaDetailsViewModel.kt` contains `castMembers = enrichment.castMembers`
    - `MetaDetailsViewModel.kt` contains `productionCompanies = enrichment.productionCompanies`
    - `MetaDetailsViewModel.kt` contains `networks = enrichment.networks`
    - `MetaDetailsViewModel.kt` contains `ageRating = enrichment.ageRating`
    - `HomeViewModelPresentationPipeline.kt` contains `externalMeta.genres`
    - `HomeViewModelPresentationPipeline.kt` contains `externalMeta.trailerYtIds`
    - `HomeDisplayMetadata.kt` contains `fun Meta.toHomeDisplayMetadata()`
    - `StreamScreenViewModel.kt` contains `val metaGenres = meta.genres`
    - `IdleScreensaverPreparation.kt` contains `externalMeta?.toHomeDisplayMetadata()`
    - `PlayerRuntimeControllerMetadata.kt` contains `sanitizeCastMembers(meta)`
    - `MetaDetailsTvdbAdvancedMetadataTest.kt` contains `detail uses tvdb cast companies networks genres and age rating`
    - `MetaDetailsTvdbAdvancedMetadataTest.kt` contains `detail does not add new tvdb metadata section`
    - `MetaSharedTvdbSurfacePropagationTest.kt` contains `home shared metadata path carries tvdb meta into existing preview fields`
    - `MetaSharedTvdbSurfacePropagationTest.kt` contains `stream screen shared metadata path reads hydrated meta genres runtime and artwork`
    - `MetaSharedTvdbSurfacePropagationTest.kt` contains `screensaver shared metadata path carries tvdb meta into slides`
    - `MetaSharedTvdbSurfacePropagationTest.kt` contains `player runtime shared metadata path reads tvdb cast members from meta`
    - `MetaDetailsScreen.kt` does not contain `TVDB Info`
    - `MetaDetailsScreen.kt` does not contain `TvdbAdvanced`
    - Targeted Gradle test command exits 0
  </acceptance_criteria>
  <done>Existing detail, Home, stream, screensaver, and player metadata surfaces receive TVDB advanced data through existing `Meta` propagation, with no new UI sections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TVDB remote metadata -> mapper/domain models | TVDB characters, companies, genres, and ratings are nullable untrusted remote data |
| Provider router -> TMDB fallback | TVDB advanced success must not silently fetch duplicate TMDB TV metadata |
| Metadata models -> UI surfaces | Remote strings and URLs feed existing detail/Home/stream/screensaver/player display roles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Denial of Service | `TvdbAdvancedMetadataMapper` | mitigate | Trim and blank-check all remote strings; tests cover nullable/blank metadata omission |
| T-09-02-02 | Information Disclosure | Provider precedence/fallback | mitigate | Add call-count tests proving TVDB active advanced success skips TMDB TV metadata calls |
| T-09-02-03 | Repudiation | `TvMetadataDiagnostics.kt` | mitigate | Add exact `tvdb_advanced_surface_success`, `tvdb_advanced_surface_missing`, and existing fallback/skipped diagnostics |
| T-09-02-04 | Tampering | `MetaDetailsViewModel` and shared metadata propagation | mitigate | Populate only existing `Meta`/`HomeDisplayMetadata` fields and preserve settings category toggles; static tests reject new TVDB UI sections and prove Home/stream/screensaver/player propagation paths |
</threat_model>

<verification>
Run:
```sh
./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.core.tvdb.TvdbAdvancedMetadataMapperTest" --tests "com.nexio.tv.core.tvdb.TvdbProviderRoutingTest" --tests "com.nexio.tv.ui.screens.detail.MetaDetailsTvdbAdvancedMetadataTest" --tests "com.nexio.tv.ui.shared.MetaSharedTvdbSurfacePropagationTest"
```
</verification>

<success_criteria>
- TVDB cast, companies, networks, genres, and content ratings replace equivalent TMDB TV surfaces when TVDB is active.
- Missing TVDB advanced data is safely omitted or explicitly falls back only through diagnostic provider routing.
- Existing detail, Home, stream, screensaver, and player metadata surfaces render TVDB data through shared `Meta` propagation; no TVDB-specific visible metadata sections are added.
</success_criteria>

<output>
After completion, create `.planning/phases/09-tvdb-advanced-tv-surfaces/09-02-SUMMARY.md`.
</output>
