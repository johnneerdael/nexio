---
phase: 09-tvdb-advanced-tv-surfaces
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataModels.kt
  - app/src/main/java/com/nexio/tv/core/tvdb/TvdbTrailerMapper.kt
  - app/src/main/java/com/nexio/tv/core/tvdb/TvdbTrailerResolver.kt
  - app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataDiagnostics.kt
  - app/src/main/java/com/nexio/tv/data/trailer/TrailerService.kt
  - app/src/main/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModel.kt
  - app/src/main/java/com/nexio/tv/ui/screens/home/HomeViewModelPresentationPipeline.kt
  - app/src/test/java/com/nexio/tv/data/trailer/TrailerServiceTvdbTest.kt
  - app/src/test/java/com/nexio/tv/ui/screens/detail/MetaDetailsTvdbTrailerTest.kt
autonomous: true
requirements:
  - META-05
user_setup: []
must_haves:
  truths:
    - "TVDB usable title trailers are tried before Streailer, fallback YouTube IDs, and explicit TMDB TV fallback"
    - "Unsupported TVDB trailer URLs are diagnosed and fallback continues without unsafe playback or external intent"
    - "Season trailers/recaps use TVDB only where they fit existing season media actions"
    - "Home/detail trailer availability and playback use the same existing trailer playback model"
  artifacts:
    - path: "app/src/main/java/com/nexio/tv/core/tvdb/TvdbTrailerMapper.kt"
      provides: "TVDB trailer URL usability classification"
      contains: "class TvdbTrailerMapper"
    - path: "app/src/main/java/com/nexio/tv/core/tvdb/TvdbTrailerResolver.kt"
      provides: "TVDB-active trailer lookup stage before TMDB fallback"
      contains: "class TvdbTrailerResolver"
    - path: "app/src/main/java/com/nexio/tv/data/trailer/TrailerService.kt"
      provides: "TVDB-first TV trailer resolution order"
      contains: "tvdbTrailerResolver"
  key_links:
    - from: "TrailerService.resolveTrailerInternal"
      to: "TvdbTrailerResolver"
      via: "TV title trailer stage before Streailer/fallback IDs/TMDB"
      pattern: "resolveTitleTrailer"
    - from: "MetaDetailsViewModel"
      to: "TrailerService"
      via: "contentId and type passed without forcing TMDB ID before TVDB"
      pattern: "resolveTrailer"
---

<objective>
Replace TMDB TV trailer discovery with TVDB-first trailer routing.

Purpose: TVDB should provide title-level TV trailers when usable, while Nexio keeps the existing playback/external model, Streailer fallback, fallback YouTube IDs, and explicit TMDB fallback only when TVDB has no usable trailer data.
Output: TVDB trailer mapper/resolver, TrailerService ordering changes, detail/Home call-site adjustments, diagnostics, and tests.
</objective>

<execution_context>
@/Users/jneerdael/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jneerdael/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-tvdb-advanced-tv-surfaces/09-CONTEXT.md
@.planning/phases/09-tvdb-advanced-tv-surfaces/09-RESEARCH.md
@.planning/phases/09-tvdb-advanced-tv-surfaces/09-VALIDATION.md
@.planning/phases/09-tvdb-advanced-tv-surfaces/09-00-SUMMARY.md
@.planning/phases/09-tvdb-advanced-tv-surfaces/09-02-SUMMARY.md
@tvdb.yml
@app/src/main/java/com/nexio/tv/data/trailer/TrailerService.kt
@app/src/main/java/com/nexio/tv/data/trailer/TrailerPlaybackSource.kt
@app/src/main/java/com/nexio/tv/data/trailer/SeasonMediaSupport.kt
@app/src/main/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModel.kt
@app/src/main/java/com/nexio/tv/ui/screens/home/HomeViewModelPresentationPipeline.kt

<interfaces>
Existing trailer result model to reuse:
```kotlin
sealed interface TrailerResolutionResult {
    data class Playback(val source: TrailerPlaybackSource) : TrailerResolutionResult
    data class External(val url: String) : TrailerResolutionResult
}
```

Required TV fallback order when TVDB is active:
```text
1. TVDB usable trailer
2. Streailer/internal sources
3. Existing fallback YouTube IDs
4. Explicit TMDB fallback only when TVDB has no usable trailer data
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TVDB trailer candidate mapping and URL safety diagnostics</name>
  <files>
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataModels.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvdbTrailerMapper.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataDiagnostics.kt,
    app/src/test/java/com/nexio/tv/data/trailer/TrailerServiceTvdbTest.kt
  </files>
  <read_first>
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataModels.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataDiagnostics.kt,
    app/src/main/java/com/nexio/tv/data/trailer/TrailerPlaybackSource.kt,
    app/src/main/java/com/nexio/tv/data/trailer/TmdbVideoFiltering.kt,
    app/src/test/java/com/nexio/tv/data/trailer/TrailerServiceTvdbTest.kt,
    .planning/phases/09-tvdb-advanced-tv-surfaces/09-CONTEXT.md,
    tvdb.yml
  </read_first>
  <behavior>
    - TVDB trailer URLs are classified as YouTube, Vimeo/external, direct playable media, or unusable
    - Only `http` and `https` schemes are allowed
    - Unsupported schemes such as `intent:`, `file:`, and `javascript:` are rejected
    - Unsupported TVDB URLs emit or carry `tvdb_trailer_unusable_url`
  </behavior>
  <action>
Add to `TvMetadataModels.kt`:
```kotlin
data class TvdbTrailerCandidate(
    val url: String,
    val name: String? = null,
    val type: String? = null,
    val seasonNumber: Int? = null,
    val isRecap: Boolean = false
)

sealed interface TvdbTrailerUsability {
    data class YouTube(val url: String, val videoId: String) : TvdbTrailerUsability
    data class External(val url: String) : TvdbTrailerUsability
    data class DirectMedia(val url: String) : TvdbTrailerUsability
    data class Unusable(val reason: String, val url: String?) : TvdbTrailerUsability
}
```

Create `TvdbTrailerMapper.kt` with `class TvdbTrailerMapper @Inject constructor()` and:
```kotlin
fun mapCandidates(series: TvdbSeriesExtendedRecord): List<TvdbTrailerCandidate>
fun classify(candidate: TvdbTrailerCandidate): TvdbTrailerUsability
```

Mapping rules per D-05 through D-08:
- Use TVDB `trailers` URL fields from `SeriesExtendedRecord.trailers`; keep nonblank name/type if present.
- Treat YouTube URLs with a valid 11-character video id as `YouTube`.
- Treat Vimeo URLs under `vimeo.com` as `External`.
- Treat direct `http`/`https` URLs ending in `.mp4`, `.m3u8`, or `.webm` as `DirectMedia`.
- Treat other `http`/`https` URLs as `External` only if they are not blank and not an unsupported scheme.
- Reject `intent:`, `file:`, `content:`, `javascript:`, and malformed URLs as `Unusable`.
- `isRecap` is true when name/type contains `recap`, case-insensitive.

In `TvMetadataDiagnostics.kt`, add:
- `TVDB_TRAILER_SUCCESS("tvdb_trailer_success")`
- `TVDB_TRAILER_MISSING("tvdb_trailer_missing")`
- `TVDB_TRAILER_UNUSABLE_URL("tvdb_trailer_unusable_url")`
- `TMDB_TRAILER_FALLBACK("tmdb_trailer_fallback")`
  </action>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.data.trailer.TrailerServiceTvdbTest"</automated>
  </verify>
  <acceptance_criteria>
    - `TvMetadataModels.kt` contains `data class TvdbTrailerCandidate`
    - `TvMetadataModels.kt` contains `sealed interface TvdbTrailerUsability`
    - `TvdbTrailerMapper.kt` contains `class TvdbTrailerMapper @Inject constructor`
    - `TvdbTrailerMapper.kt` contains `fun mapCandidates(`
    - `TvdbTrailerMapper.kt` contains `fun classify(`
    - `TvdbTrailerMapper.kt` contains `javascript:`
    - `TvdbTrailerMapper.kt` contains `.m3u8`
    - `TvMetadataDiagnostics.kt` contains `TVDB_TRAILER_UNUSABLE_URL("tvdb_trailer_unusable_url")`
    - `TvMetadataDiagnostics.kt` contains `TMDB_TRAILER_FALLBACK("tmdb_trailer_fallback")`
    - Targeted Gradle test command exits 0 after Task 2 implementation
  </acceptance_criteria>
  <done>TVDB trailer records have a safe, tested usability model before TrailerService consumes them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Insert TVDB trailer resolver before Streailer and TMDB fallback</name>
  <files>
    app/src/main/java/com/nexio/tv/core/tvdb/TvdbTrailerResolver.kt,
    app/src/main/java/com/nexio/tv/data/trailer/TrailerService.kt,
    app/src/test/java/com/nexio/tv/data/trailer/TrailerServiceTvdbTest.kt
  </files>
  <read_first>
    app/src/main/java/com/nexio/tv/core/tvdb/TvdbTrailerMapper.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvdbMetadataService.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataRouter.kt,
    app/src/main/java/com/nexio/tv/data/trailer/TrailerService.kt,
    app/src/main/java/com/nexio/tv/data/trailer/TrailerPlaybackSource.kt,
    app/src/test/java/com/nexio/tv/data/trailer/TrailerServiceTvdbTest.kt,
    .planning/phases/09-tvdb-advanced-tv-surfaces/09-CONTEXT.md
  </read_first>
  <behavior>
    - Title TV trailer resolution order is TVDB, Streailer, fallback YouTube IDs, explicit TMDB
    - TVDB YouTube trailers resolve through existing YouTube extractor path
    - TVDB direct media produces `TrailerResolutionResult.Playback`
    - TVDB external URL produces `TrailerResolutionResult.External`
    - TMDB TV videos are called only after TVDB has no usable trailer and other fallbacks miss
  </behavior>
  <action>
Create `TvdbTrailerResolver.kt` with `class TvdbTrailerResolver @Inject constructor(...)`. It may depend on Phase 7 `TvdbSettingsDataStore`, `TvdbIdentityService`, `TvdbMetadataService`, and `TvdbTrailerMapper`, or it may consume `TvMetadataRouter` if Phase 7 exposes TVDB-only enrichment without falling back to TMDB. Do not depend on `TmdbApi` or `TmdbMetadataService`.

Expose:
```kotlin
suspend fun resolveTitleTrailer(contentId: String?, type: String?, title: String?, year: String?): TvdbTrailerLookupResult
suspend fun resolveSeasonTrailer(contentId: String?, type: String?, seasonNumber: Int?, title: String?, year: String?): TvdbTrailerLookupResult
suspend fun resolveSeasonRecap(contentId: String?, type: String?, seasonNumber: Int?, title: String?, year: String?): TvdbTrailerLookupResult
```

Define `TvdbTrailerLookupResult` as:
```kotlin
sealed interface TvdbTrailerLookupResult {
    data object Inactive : TvdbTrailerLookupResult
    data object Missing : TvdbTrailerLookupResult
    data class Unusable(val reason: String) : TvdbTrailerLookupResult
    data class Resolved(val result: TrailerResolutionResult) : TvdbTrailerLookupResult
}
```

Update `TrailerService` constructor to inject `TvdbTrailerResolver`.

Change `resolveTrailerInternal` for TV only (`normalizeTmdbMediaType(type) == "tv"`):
1. Call `tvdbTrailerResolver.resolveTitleTrailer(contentId, type, title, year)`.
2. If `Resolved`, return it.
3. Then call `resolveStreailerTrailer(...)`.
4. Then try `fallbackYtIds` through `resolveYouTubeTrailer`.
5. Then call `resolveTrailerPlaybackFromTmdbId(...)` only as explicit fallback and log/diagnose `tmdb_trailer_fallback`.

Leave movie ordering unchanged. This means movies still use TMDB trailer discovery first. This implements D-07 only for TV paths.

For `getTitleMediaAvailability`, return true for TVDB resolved title trailer before TMDB TV videos per D-05. For TV, do not call `fetchTmdbTvVideos` until TVDB, Streailer, and fallback IDs are unavailable.
  </action>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.data.trailer.TrailerServiceTvdbTest"</automated>
  </verify>
  <acceptance_criteria>
    - `TvdbTrailerResolver.kt` contains `class TvdbTrailerResolver @Inject constructor`
    - `TvdbTrailerResolver.kt` contains `sealed interface TvdbTrailerLookupResult`
    - `TrailerService.kt` contains `tvdbTrailerResolver: TvdbTrailerResolver`
    - `TrailerService.kt` contains `resolveTitleTrailer`
    - `TrailerService.kt` contains `tmdb_trailer_fallback` or `TMDB_TRAILER_FALLBACK`
    - `TrailerServiceTvdbTest.kt` contains `tvdb trailer is tried before streailer fallback ids and tmdb`
    - `TrailerServiceTvdbTest.kt` contains `coVerify(exactly = 0) { tmdbApi.getTvVideos`
    - `TrailerServiceTvdbTest.kt` contains `tmdb tv trailer fallback runs only when tvdb has no usable trailer`
    - Targeted Gradle test command exits 0
  </acceptance_criteria>
  <done>TV title trailer resolution follows the locked TVDB-first fallback order and keeps movie trailer behavior unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire existing detail and Home trailer call sites to avoid pre-TMDB TV lookup</name>
  <files>
    app/src/main/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModel.kt,
    app/src/main/java/com/nexio/tv/ui/screens/home/HomeViewModelPresentationPipeline.kt,
    app/src/test/java/com/nexio/tv/ui/screens/detail/MetaDetailsTvdbTrailerTest.kt,
    app/src/test/java/com/nexio/tv/data/trailer/TrailerServiceTvdbTest.kt
  </files>
  <read_first>
    app/src/main/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModel.kt,
    app/src/main/java/com/nexio/tv/ui/screens/home/HomeViewModelPresentationPipeline.kt,
    app/src/main/java/com/nexio/tv/data/trailer/TrailerService.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvdbTrailerResolver.kt,
    app/src/test/java/com/nexio/tv/data/trailer/TrailerServiceTvdbTest.kt,
    .planning/phases/09-tvdb-advanced-tv-surfaces/09-CONTEXT.md
  </read_first>
  <behavior>
    - Detail title trailer availability passes content id/type/fallback IDs without resolving TMDB ID first for TV
    - Detail trailer button can receive TVDB external or playback result through existing state fields
    - Season trailer/recap actions call TVDB resolver only through existing season media actions
    - Home trailer preview passes content id/type to `TrailerService` so TVDB can win before TMDB TV videos
  </behavior>
  <action>
Update `MetaDetailsViewModel` per D-06 and D-08:
- In `preloadTitleTrailerAvailability`, for `ContentType.SERIES`/`TV`, pass `tmdbId = null` into `trailerService.getTitleMediaAvailability` and include `contentId = meta.id`, `type = meta.apiType`, and `fallbackYtIds = meta.trailerYtIds`. Keep current TMDB ID lookup for movies.
- In `fetchTrailer`, for TV content, pass `tmdbId = null` to `trailerService.resolveTrailer`; keep `contentId`, `type`, selected season when applicable, and fallback YouTube IDs. Keep movie TMDB lookup unchanged.
- In `fetchSeasonTrailer` and `fetchSeasonRecap`, pass `tmdbId = null` for TV content so `TrailerService` can try TVDB season trailer/recap before TMDB. Do not add new season buttons or new season actions.

Update `HomeViewModelPresentationPipeline.kt` trailer preview calls similarly: for TV/series, do not call `tmdbService.ensureTmdbId` before `TrailerService`; pass `contentId`, `type`, and fallback YouTube IDs. Keep movie preview behavior unchanged.

Add `MetaDetailsTvdbTrailerTest.kt` with tests named:
- `detail title trailer availability does not resolve tmdb before tvdb`
- `season trailer action still uses existing action surface`

Do not add a new TVDB trailer button, TVDB recap button, or new UI state field per D-06.
  </action>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.data.trailer.TrailerServiceTvdbTest" --tests "com.nexio.tv.ui.screens.detail.MetaDetailsTvdbTrailerTest"</automated>
  </verify>
  <acceptance_criteria>
    - `MetaDetailsViewModel.kt` contains `getTitleMediaAvailability(`
    - `MetaDetailsViewModel.kt` contains `resolveTrailer(`
    - `MetaDetailsViewModel.kt` contains `tmdbId = if (parseApiTypeToContentType(meta.apiType) == ContentType.SERIES) null`
    - `HomeViewModelPresentationPipeline.kt` contains `type = item.apiType`
    - `HomeViewModelPresentationPipeline.kt` contains `contentId = item.id`
    - `MetaDetailsTvdbTrailerTest.kt` contains `detail title trailer availability does not resolve tmdb before tvdb`
    - `MetaDetailsTvdbTrailerTest.kt` contains `season trailer action still uses existing action surface`
    - `MetaDetailsViewModel.kt` does not contain `OnPlayTvdbTrailer`
    - Targeted Gradle test command exits 0
  </acceptance_criteria>
  <done>Existing detail and Home trailer UX uses TVDB-first TV trailer routing without adding new media actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TVDB trailer URL -> playback/external intent | Remote trailer URLs can be malformed, unsupported, or malicious |
| TrailerService -> TMDB TV fallback | TVDB-active TV success must not silently fetch TMDB TV videos first |
| Detail/Home UI -> trailer service | UI passes route/content IDs that may be malformed or provider-prefixed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Tampering / Elevation of Privilege | `TvdbTrailerMapper` | mitigate | Accept only `http`/`https`; reject `intent:`, `file:`, `content:`, and `javascript:` URLs; tests cover unsupported URL fallback |
| T-09-03-02 | Information Disclosure | `TrailerService` provider precedence | mitigate | TVDB active title success skips TMDB TV videos; tests assert zero `tmdbApi.getTvVideos` calls |
| T-09-03-03 | Repudiation | Trailer diagnostics | mitigate | Emit exact `tvdb_trailer_success`, `tvdb_trailer_missing`, `tvdb_trailer_unusable_url`, and `tmdb_trailer_fallback` diagnostics/logs |
| T-09-03-04 | Denial of Service | Trailer URL parsing | mitigate | Malformed TVDB URLs classify as unusable and fallback continues without throwing |
</threat_model>

<verification>
Run:
```sh
./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.data.trailer.TrailerServiceTvdbTest" --tests "com.nexio.tv.ui.screens.detail.MetaDetailsTvdbTrailerTest"
```
</verification>

<success_criteria>
- TVDB title trailers replace TMDB TV trailer discovery when usable.
- TV fallback order is exactly TVDB, Streailer, fallback YouTube IDs, explicit TMDB fallback.
- Season trailers/recaps use TVDB only through existing actions.
- Unsafe/unusable trailer URLs are diagnosed and do not block fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/09-tvdb-advanced-tv-surfaces/09-03-SUMMARY.md`.
</output>
