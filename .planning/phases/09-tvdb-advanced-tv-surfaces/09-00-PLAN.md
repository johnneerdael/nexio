---
phase: 09-tvdb-advanced-tv-surfaces
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/test/java/com/nexio/tv/core/tvdb/TvdbSeasonOrderMapperTest.kt
  - app/src/test/java/com/nexio/tv/core/tvdb/TvdbAdvancedMetadataMapperTest.kt
  - app/src/test/java/com/nexio/tv/core/tvdb/TvdbProviderRoutingTest.kt
  - app/src/test/java/com/nexio/tv/data/trailer/TrailerServiceTvdbTest.kt
  - app/src/test/java/com/nexio/tv/ui/screens/detail/MetaDetailsTvdbSeasonOrderTest.kt
  - app/src/test/java/com/nexio/tv/ui/screens/settings/TvdbSettingsNoAdvancedToggleTest.kt
autonomous: true
requirements:
  - META-03
  - META-05
  - UX-02
user_setup: []
must_haves:
  truths:
    - "Phase 9 implementation does not start unless Phase 7 TVDB provider-router source files exist"
    - "Missing Phase 9 validation tests are created before implementation work starts"
    - "Tests encode canonical Trakt season/episode stability, TVDB advanced surface replacement, trailer precedence, skipped TMDB TV calls, and no extra TVDB toggles"
  artifacts:
    - path: "app/src/test/java/com/nexio/tv/core/tvdb/TvdbSeasonOrderMapperTest.kt"
      provides: "META-03 mapper contract for default season type preservation"
      contains: "canonical season and episode stay unchanged"
    - path: "app/src/test/java/com/nexio/tv/data/trailer/TrailerServiceTvdbTest.kt"
      provides: "META-05 trailer precedence contract"
      contains: "tvdb trailer is tried before streailer fallback ids and tmdb"
    - path: "app/src/test/java/com/nexio/tv/ui/screens/settings/TvdbSettingsNoAdvancedToggleTest.kt"
      provides: "UX-02 static guard against provider-specific advanced/timing toggles"
      contains: "does not add tvdb advanced or exact timing toggle"
  key_links:
    - from: "Phase 9 tests"
      to: "Phase 7 provider outputs"
      via: "prerequisite existence gate"
      pattern: "TvMetadataRouter.kt"
    - from: "TvdbProviderRoutingTest"
      to: "TmdbMetadataService"
      via: "call-count assertions"
      pattern: "coVerify\\(exactly = 0\\)"
---

<objective>
Create the Wave 0 validation scaffold for Phase 9.

Purpose: Phase 9 must be driven by the validation contract in `09-VALIDATION.md`, and implementation must not begin on top of an unexecuted Phase 7 provider foundation.
Output: Missing test classes for season ordering, advanced metadata, trailer routing, provider call-count behavior, detail season stability, and no extra TVDB toggles.
</objective>

<execution_context>
@/Users/jneerdael/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jneerdael/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-tvdb-advanced-tv-surfaces/09-CONTEXT.md
@.planning/phases/09-tvdb-advanced-tv-surfaces/09-RESEARCH.md
@.planning/phases/09-tvdb-advanced-tv-surfaces/09-VALIDATION.md
@.planning/phases/07-tvdb-provider-replacement/07-01-PLAN.md
@.planning/phases/07-tvdb-provider-replacement/07-02-PLAN.md
@.planning/phases/07-tvdb-provider-replacement/07-03-PLAN.md
@.planning/phases/07-tvdb-provider-replacement/07-04-PLAN.md
@.planning/phases/07-tvdb-provider-replacement/07-05-PLAN.md
@.planning/phases/07-tvdb-provider-replacement/07-06-PLAN.md
@tvdb.yml

<interfaces>
Phase 7 source files that must exist before Phase 9 implementation:
```text
app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataModels.kt
app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataDiagnostics.kt
app/src/main/java/com/nexio/tv/core/tvdb/TvdbMetadataService.kt
app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataRouter.kt
app/src/main/java/com/nexio/tv/data/remote/api/TvdbApi.kt
app/src/main/java/com/nexio/tv/ui/screens/settings/TvdbSettingsScreen.kt
```

Existing contracts Phase 9 must preserve:
```kotlin
data class Video(
    val season: Int?,
    val episode: Int?
)

data class WatchProgress(
    val season: Int?,
    val episode: Int?
)

internal fun buildEpisodesForSeason(videos: List<Video>, season: Int): List<Video> =
    videos.filter { it.season == season }.sortedBy { it.episode }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate on Phase 7 TVDB provider outputs</name>
  <files>None</files>
  <read_first>
    .planning/phases/09-tvdb-advanced-tv-surfaces/09-CONTEXT.md,
    .planning/phases/09-tvdb-advanced-tv-surfaces/09-VALIDATION.md,
    .planning/phases/07-tvdb-provider-replacement/07-01-PLAN.md,
    .planning/phases/07-tvdb-provider-replacement/07-02-PLAN.md,
    .planning/phases/07-tvdb-provider-replacement/07-03-PLAN.md,
    .planning/phases/07-tvdb-provider-replacement/07-04-PLAN.md,
    .planning/phases/07-tvdb-provider-replacement/07-05-PLAN.md,
    .planning/phases/07-tvdb-provider-replacement/07-06-PLAN.md,
    tvdb.yml
  </read_first>
  <action>
Before creating tests or app changes, run these exact existence checks:

`test -f app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataModels.kt`
`test -f app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataDiagnostics.kt`
`test -f app/src/main/java/com/nexio/tv/core/tvdb/TvdbMetadataService.kt`
`test -f app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataRouter.kt`
`test -f app/src/main/java/com/nexio/tv/data/remote/api/TvdbApi.kt`
`test -f app/src/main/java/com/nexio/tv/ui/screens/settings/TvdbSettingsScreen.kt`

Then run `rg -n "class TvMetadataRouter|class TvdbMetadataService|data class TvMetadataEnrichment|enum class TvMetadataDecisionReason|TMDB_TV_SKIPPED|getSeriesExtended|getSeriesEpisodes" app/src/main/java app/src/test/java`.

If any required file is absent, stop before Task 2 and return `## CHECKPOINT REACHED` with this blocker: "Phase 9 is blocked because Phase 7 TVDB provider replacement source is absent. Execute Phase 7 first or merge the branch that provides TvMetadataModels, TvMetadataDiagnostics, TvdbMetadataService, TvMetadataRouter, TvdbApi metadata endpoints, and TvdbSettingsScreen." Do not create replacement TVDB auth, identity, or Phase 7 provider-router code in Phase 9.

Do not read or print `.thetvdb.apikey`.
  </action>
  <verify>
    <automated>test -f app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataModels.kt && test -f app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataDiagnostics.kt && test -f app/src/main/java/com/nexio/tv/core/tvdb/TvdbMetadataService.kt && test -f app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataRouter.kt && test -f app/src/main/java/com/nexio/tv/data/remote/api/TvdbApi.kt && test -f app/src/main/java/com/nexio/tv/ui/screens/settings/TvdbSettingsScreen.kt</automated>
  </verify>
  <acceptance_criteria>
    - CLI output confirms all six required Phase 7 source files exist before Task 2 starts
    - If any required file is missing, the executor returns `## CHECKPOINT REACHED` and the exact blocker string from the action
    - No Phase 9 task creates new TVDB credential, login, token-store, or identity-resolution code
    - The executor does not read or print `.thetvdb.apikey`
  </acceptance_criteria>
  <done>Phase 7 source availability is proven, or Phase 9 is blocked before writing tests or app code.</done>
</task>

<task type="auto">
  <name>Task 2: Add season-order and provider-routing failing tests</name>
  <files>
    app/src/test/java/com/nexio/tv/core/tvdb/TvdbSeasonOrderMapperTest.kt,
    app/src/test/java/com/nexio/tv/core/tvdb/TvdbProviderRoutingTest.kt,
    app/src/test/java/com/nexio/tv/ui/screens/detail/MetaDetailsTvdbSeasonOrderTest.kt
  </files>
  <read_first>
    app/src/main/java/com/nexio/tv/domain/model/Meta.kt,
    app/src/main/java/com/nexio/tv/domain/model/WatchProgress.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataModels.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataRouter.kt,
    app/src/main/java/com/nexio/tv/ui/screens/detail/MetaDetailsUiState.kt,
    app/src/test/java/com/nexio/tv/ui/screens/detail/MetaDetailsNavigationSupportTest.kt,
    .planning/phases/09-tvdb-advanced-tv-surfaces/09-VALIDATION.md,
    tvdb.yml
  </read_first>
  <action>
Create `TvdbSeasonOrderMapperTest.kt` in package `com.nexio.tv.core.tvdb` with tests named exactly:
- `preserves default season type and available season types`
- `canonical season and episode stay unchanged when default order differs`
- `specials placement is preserved but not applied to canonical keys`

Those tests must assert that a mapped episode with canonical `(seasonNumber = 1, number = 5)` and TVDB default order `(defaultSeason = 2, defaultEpisode = 3)` keeps `Video.season == 1` and `Video.episode == 5`, while `Video.tvdbEpisodeOrder.defaultSeason == 2` and `Video.tvdbEpisodeOrder.defaultEpisode == 3` after Plan 09-01 implementation.

Create `TvdbProviderRoutingTest.kt` with a test named `advanced tvdb success records skipped tmdb tv call`. The test must mock `TmdbService` and `TmdbMetadataService` and assert `coVerify(exactly = 0)` for `ensureTmdbId`, `fetchEnrichment`, and `fetchEpisodeEnrichment` when TVDB advanced metadata succeeds.

Create `MetaDetailsTvdbSeasonOrderTest.kt` in package `com.nexio.tv.ui.screens.detail` with tests named exactly:
- `season tabs continue to use canonical video seasons`
- `episode progress map still matches canonical season episode pairs`

Use `MetaDetailsUiState.withRefreshedMeta`, `buildEpisodesForSeason`, and a `WatchProgress` map keyed by `(1 to 5)` to prove TVDB alternate/default order metadata does not change existing season tabs or progress keys.
  </action>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.core.tvdb.TvdbSeasonOrderMapperTest" --tests "com.nexio.tv.core.tvdb.TvdbProviderRoutingTest" --tests "com.nexio.tv.ui.screens.detail.MetaDetailsTvdbSeasonOrderTest"</automated>
  </verify>
  <acceptance_criteria>
    - `TvdbSeasonOrderMapperTest.kt` contains `preserves default season type and available season types`
    - `TvdbSeasonOrderMapperTest.kt` contains `canonical season and episode stay unchanged`
    - `TvdbSeasonOrderMapperTest.kt` contains `specials placement is preserved but not applied`
    - `TvdbProviderRoutingTest.kt` contains `advanced tvdb success records skipped tmdb tv call`
    - `TvdbProviderRoutingTest.kt` contains `coVerify(exactly = 0)`
    - `MetaDetailsTvdbSeasonOrderTest.kt` contains `season tabs continue to use canonical video seasons`
    - `MetaDetailsTvdbSeasonOrderTest.kt` contains `episode progress map still matches canonical season episode pairs`
  </acceptance_criteria>
  <done>Wave 0 tests define META-03 season-order preservation and skipped-TMDB provider-routing behavior before implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Add advanced metadata, trailer, and no-toggle failing tests</name>
  <files>
    app/src/test/java/com/nexio/tv/core/tvdb/TvdbAdvancedMetadataMapperTest.kt,
    app/src/test/java/com/nexio/tv/data/trailer/TrailerServiceTvdbTest.kt,
    app/src/test/java/com/nexio/tv/ui/screens/settings/TvdbSettingsNoAdvancedToggleTest.kt
  </files>
  <read_first>
    app/src/main/java/com/nexio/tv/domain/model/Meta.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataModels.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataDiagnostics.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvdbMetadataService.kt,
    app/src/main/java/com/nexio/tv/data/trailer/TrailerService.kt,
    app/src/main/java/com/nexio/tv/data/trailer/TrailerPlaybackSource.kt,
    app/src/main/java/com/nexio/tv/ui/screens/settings/TvdbSettingsScreen.kt,
    app/src/main/res/values/strings.xml,
    .planning/phases/09-tvdb-advanced-tv-surfaces/09-VALIDATION.md,
    tvdb.yml
  </read_first>
  <action>
Create `TvdbAdvancedMetadataMapperTest.kt` with tests named exactly:
- `maps characters companies networks genres and content ratings`
- `blank nullable advanced metadata is omitted safely`
- `content rating prefers locale country before first available rating`

The first test must assert TVDB character `personName = "Anna Torv"`, `name = "Olivia Dunham"`, and `personImgURL` map to `MetaCastMember(name = "Anna Torv", character = "Olivia Dunham", photo = personImgURL)`, `originalNetwork` maps to `MetaCompanyKind.NETWORK`, `companies` maps to `MetaCompanyKind.COMPANY`, genres are nonblank strings, and age rating selects the preferred country.

Create `TrailerServiceTvdbTest.kt` with tests named exactly:
- `tvdb trailer is tried before streailer fallback ids and tmdb`
- `tmdb tv trailer fallback runs only when tvdb has no usable trailer`
- `unsupported tvdb trailer url is diagnosed and fallback continues`

The first trailer test must assert `coVerify(exactly = 0) { tmdbApi.getTvVideos(any(), any(), any()) }` when a TVDB YouTube trailer resolves. The fallback test must assert one TMDB TV videos call only after TVDB has no usable trailer and Streailer/fallback IDs miss.

Create `TvdbSettingsNoAdvancedToggleTest.kt` with a file/static assertion test named `does not add tvdb advanced or exact timing toggle`. It must read `TvdbSettingsScreen.kt` and `strings.xml` and fail if they contain any of these case-insensitive phrases: `Use TVDB advanced metadata`, `TVDB advanced toggle`, `Use TVDB timing`, `Exact air-time toggle`, `provider-specific timing`.
  </action>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.core.tvdb.TvdbAdvancedMetadataMapperTest" --tests "com.nexio.tv.data.trailer.TrailerServiceTvdbTest" --tests "com.nexio.tv.ui.screens.settings.TvdbSettingsNoAdvancedToggleTest"</automated>
  </verify>
  <acceptance_criteria>
    - `TvdbAdvancedMetadataMapperTest.kt` contains `maps characters companies networks genres and content ratings`
    - `TvdbAdvancedMetadataMapperTest.kt` contains `blank nullable advanced metadata is omitted safely`
    - `TvdbAdvancedMetadataMapperTest.kt` contains `content rating prefers locale country`
    - `TrailerServiceTvdbTest.kt` contains `tvdb trailer is tried before streailer fallback ids and tmdb`
    - `TrailerServiceTvdbTest.kt` contains `tmdb tv trailer fallback runs only when tvdb has no usable trailer`
    - `TrailerServiceTvdbTest.kt` contains `unsupported tvdb trailer url is diagnosed and fallback continues`
    - `TvdbSettingsNoAdvancedToggleTest.kt` contains `does not add tvdb advanced or exact timing toggle`
    - `TvdbSettingsNoAdvancedToggleTest.kt` contains `Use TVDB timing`
  </acceptance_criteria>
  <done>Wave 0 tests define META-05 advanced/trailer replacement and UX-02 no-provider-toggle behavior before implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 7 provider source -> Phase 9 tests | Phase 9 must consume Phase 7 provider routing and must not recreate credentials, auth, or identity code |
| Test fixtures -> repository | Fixtures must not contain TVDB API keys, bearer tokens, subscriber PINs, or copied live API payload secrets |
| Static settings tests -> UX contract | Provider-specific timing/advanced toggles would violate UX-02 and user decisions D-13/D-14 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-00-01 | Information Disclosure | Phase prerequisite gate/tests | mitigate | Do not read `.thetvdb.apikey`; use mocked TVDB DTOs and fake URLs only |
| T-09-00-02 | Repudiation | `TvdbProviderRoutingTest` | mitigate | Add call-count assertions and diagnostic assertions for `tmdb_tv_skipped` on TVDB advanced success |
| T-09-00-03 | Tampering | `TrailerServiceTvdbTest` | mitigate | Encode supported URL rules and fallback order before implementation so unsafe/unusable URLs continue fallback |
| T-09-00-04 | Denial of Service | Advanced metadata fixtures | mitigate | Test nullable/blank TVDB data paths so mapper implementation omits bad data instead of crashing |
</threat_model>

<verification>
Run:
```sh
./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.core.tvdb.TvdbSeasonOrderMapperTest" --tests "com.nexio.tv.core.tvdb.TvdbAdvancedMetadataMapperTest" --tests "com.nexio.tv.core.tvdb.TvdbProviderRoutingTest" --tests "com.nexio.tv.data.trailer.TrailerServiceTvdbTest" --tests "com.nexio.tv.ui.screens.detail.MetaDetailsTvdbSeasonOrderTest" --tests "com.nexio.tv.ui.screens.settings.TvdbSettingsNoAdvancedToggleTest"
```
</verification>

<success_criteria>
- Phase 7 TVDB provider source files exist before implementation proceeds.
- Every missing test class listed in `09-VALIDATION.md` exists.
- Tests express the exact Phase 9 contracts and may fail until later plans implement them.
- No live TVDB secret is read or committed.
</success_criteria>

<output>
After completion, create `.planning/phases/09-tvdb-advanced-tv-surfaces/09-00-SUMMARY.md`.
</output>
