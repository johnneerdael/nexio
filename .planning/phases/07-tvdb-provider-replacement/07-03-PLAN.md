---
phase: 07-tvdb-provider-replacement
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - app/src/main/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModel.kt
  - app/src/test/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModelTestFactory.kt
  - app/src/test/java/com/nexio/tv/ui/screens/detail/MetaDetailsTvdbProviderRoutingTest.kt
  - app/src/test/java/com/nexio/tv/ui/screens/detail/MarkSeasonWatchedTest.kt
autonomous: true
requirements:
  - PREF-02
  - PREF-03
  - META-01
  - META-02
  - META-04
must_haves:
  truths:
    - "TV detail enrichment for series uses `TvMetadataRouter` before any TMDB TV metadata lookup"
    - "TV detail episode metadata maps TVDB episode title, overview, thumbnail, runtime, and aired date into `Video` rows"
    - "Mark-season-watched uses TVDB season episodes for series when TVDB succeeds and preserves existing date-only `AirDateGate` behavior"
    - "Movie detail enrichment remains TMDB-backed"
  artifacts:
    - path: "app/src/main/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModel.kt"
      provides: "Detail screen TVDB-first metadata consumption"
      contains: "tvMetadataRouter"
    - path: "app/src/test/java/com/nexio/tv/ui/screens/detail/MetaDetailsTvdbProviderRoutingTest.kt"
      provides: "Representative detail no-TMDB-call test"
      contains: "does not call tmdb"
  key_links:
    - from: "MetaDetailsViewModel.enrichMeta"
      to: "TvMetadataRouter.fetchEnrichment"
      via: "series detail enrichment"
      pattern: "fetchEnrichment\\("
    - from: "MetaDetailsViewModel.markSeasonWatched"
      to: "TvMetadataRouter.fetchSeasonEpisodes"
      via: "authoritative season episode list"
      pattern: "fetchSeasonEpisodes"
user_setup: []
---

<objective>
Route Detail screen TV metadata through the TVDB-first router.

Purpose: Detail is the highest-value representative path for proving normal TVDB-enabled TV metadata success does not issue duplicate TMDB TV metadata calls.
Output: Detail ViewModel router integration and tests for enrichment, episodes, and season watched behavior.
</objective>

<execution_context>
@/Users/jneerdael/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jneerdael/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-tvdb-provider-replacement/07-CONTEXT.md
@.planning/phases/07-tvdb-provider-replacement/07-RESEARCH.md
@.planning/phases/07-tvdb-provider-replacement/07-VALIDATION.md
@app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataRouter.kt
@app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataModels.kt
@app/src/main/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModel.kt
@app/src/test/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModelTestFactory.kt
@app/src/test/java/com/nexio/tv/ui/screens/detail/MarkSeasonWatchedTest.kt

<interfaces>
Use these router methods:
```kotlin
suspend fun TvMetadataRouter.fetchEnrichment(request: TvMetadataRequest): TvMetadataDecision<TvMetadataEnrichment>
suspend fun TvMetadataRouter.fetchEpisodeEnrichment(request: TvMetadataRequest): TvMetadataDecision<Map<Pair<Int, Int>, TvEpisodeMetadata>>
suspend fun TvMetadataRouter.fetchSeasonEpisodes(contentId: String, fallbackContentId: String?, seasonNumber: Int, language: String? = null): TvMetadataDecision<List<TvSeasonEpisode>>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject router and route detail series enrichment</name>
  <files>
    app/src/main/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModel.kt,
    app/src/test/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModelTestFactory.kt,
    app/src/test/java/com/nexio/tv/ui/screens/detail/MetaDetailsTvdbProviderRoutingTest.kt
  </files>
  <read_first>
    app/src/main/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModel.kt,
    app/src/test/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModelTestFactory.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataRouter.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataModels.kt,
    app/src/main/java/com/nexio/tv/core/tmdb/TmdbMetadataService.kt,
    .planning/phases/07-tvdb-provider-replacement/07-CONTEXT.md
  </read_first>
  <behavior>
    - For `ContentType.SERIES` or `ContentType.TV`, `enrichMeta` calls `tvMetadataRouter.fetchEnrichment`
    - TVDB success applies artwork/basic/details fields using existing `TmdbSettings` intent toggles
    - TVDB success never calls `tmdbService.ensureTmdbId` or `tmdbMetadataService.fetchEnrichment`
    - Movie enrichment still uses existing TMDB branch
  </behavior>
  <action>
Add `private val tvMetadataRouter: TvMetadataRouter` to `MetaDetailsViewModel` constructor and update `MetaDetailsViewModelTestFactory.kt` to provide a relaxed mock by default.

In `enrichMeta(meta: Meta)`, keep the existing `TmdbSettingsDataStore` toggles. Split routing by `resolveTmdbContentType(meta)`:
- For movie content, keep the current TMDB flow unchanged.
- For `ContentType.SERIES` or `ContentType.TV`, do not call `tmdbService.ensureTmdbId` before router success/fallback. Call:
```kotlin
val decision = tvMetadataRouter.fetchEnrichment(
    TvMetadataRequest(
        contentId = meta.id,
        fallbackContentId = itemId,
        contentType = tmdbContentType
    )
)
val enrichment = decision.value
```

Apply `TvMetadataEnrichment` into `Meta` with the same toggle groups already present: `useArtwork` controls `background` and `logo`, `useBasicInfo` controls `name`, `description`, `genres`, and rating, and `useDetails` controls `runtime`, `releaseInfo`, `ageRating`, `country`, and `language`. Do not add new UI fields for `airsTime`, aliases, remote IDs, season type, trailers, cast/company/network discovery, or exact air-time behavior.
  </action>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.ui.screens.detail.MetaDetailsTvdbProviderRoutingTest"</automated>
  </verify>
  <acceptance_criteria>
    - `MetaDetailsViewModel.kt` contains `private val tvMetadataRouter: TvMetadataRouter`
    - `MetaDetailsViewModel.kt` contains `TvMetadataRequest(`
    - `MetaDetailsViewModel.kt` contains `tvMetadataRouter.fetchEnrichment`
    - `MetaDetailsViewModelTestFactory.kt` contains `tvMetadataRouter: TvMetadataRouter`
    - `MetaDetailsTvdbProviderRoutingTest.kt` contains `does not call tmdb`
    - `MetaDetailsTvdbProviderRoutingTest.kt` contains `coVerify(exactly = 0) { tmdbService.ensureTmdbId`
    - Targeted Gradle test command exits 0
  </acceptance_criteria>
  <done>Series detail enrichment is TVDB-first through the router, while movie enrichment remains on TMDB.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route detail episode enrichment through TVDB</name>
  <files>
    app/src/main/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModel.kt,
    app/src/test/java/com/nexio/tv/ui/screens/detail/MetaDetailsTvdbProviderRoutingTest.kt
  </files>
  <read_first>
    app/src/main/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModel.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataRouter.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataModels.kt,
    app/src/main/java/com/nexio/tv/domain/model/Meta.kt,
    app/src/test/java/com/nexio/tv/ui/screens/detail/MetaDetailsSeasonMediaViewModelTest.kt,
    .planning/phases/07-tvdb-provider-replacement/07-VALIDATION.md
  </read_first>
  <behavior>
    - `settings.useEpisodes` and series content calls `tvMetadataRouter.fetchEpisodeEnrichment`
    - TVDB episode metadata updates `Video.title`, `Video.overview`, `Video.released`, `Video.thumbnail`, and `Video.runtime`
    - Episode enrichment success does not call `tmdbMetadataService.fetchEpisodeEnrichment`
  </behavior>
  <action>
Replace only the series episode-enrichment branch inside `enrichMeta`. When `settings.useEpisodes` is true and `meta.apiType` is `series` or `tv`, compute `seasonNumbers = meta.videos.mapNotNull { it.season }.distinct()` and call:
```kotlin
val episodeDecision = tvMetadataRouter.fetchEpisodeEnrichment(
    TvMetadataRequest(
        contentId = meta.id,
        fallbackContentId = itemId,
        contentType = tmdbContentType,
        seasonNumbers = seasonNumbers
    )
)
val episodeMap = episodeDecision.value.orEmpty()
```

Map each `TvEpisodeMetadata` to `Video` exactly as the current TMDB branch does: title, overview, released, thumbnail, and runtime. Keep movie episode handling unchanged. Do not implement Phase 8 exact airing instants; keep `released` as the date string supplied by TVDB.
  </action>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.ui.screens.detail.MetaDetailsTvdbProviderRoutingTest"</automated>
  </verify>
  <acceptance_criteria>
    - `MetaDetailsViewModel.kt` contains `tvMetadataRouter.fetchEpisodeEnrichment`
    - `MetaDetailsViewModel.kt` contains `seasonNumbers = meta.videos.mapNotNull`
    - `MetaDetailsTvdbProviderRoutingTest.kt` contains `updates episode rows from tvdb`
    - `MetaDetailsTvdbProviderRoutingTest.kt` contains `coVerify(exactly = 0) { tmdbMetadataService.fetchEpisodeEnrichment`
    - Targeted Gradle test command exits 0
  </acceptance_criteria>
  <done>Detail episode rows use TVDB episode metadata in normal TVDB success paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route mark-season-watched authoritative episode list through TVDB</name>
  <files>
    app/src/main/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModel.kt,
    app/src/test/java/com/nexio/tv/ui/screens/detail/MarkSeasonWatchedTest.kt
  </files>
  <read_first>
    app/src/main/java/com/nexio/tv/ui/screens/detail/MetaDetailsViewModel.kt,
    app/src/test/java/com/nexio/tv/ui/screens/detail/MarkSeasonWatchedTest.kt,
    app/src/main/java/com/nexio/tv/data/repository/AirDateGate.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataRouter.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvMetadataModels.kt,
    .planning/phases/07-tvdb-provider-replacement/07-CONTEXT.md
  </read_first>
  <behavior>
    - Series mark-season-watched calls `tvMetadataRouter.fetchSeasonEpisodes`
    - TVDB season episodes are filtered through existing `AirDateGate.isAired(0L, airDate, nowMs)`
    - TVDB success does not call `tmdbService.ensureTmdbId` or `tmdbMetadataService.fetchSeasonEpisodes`
    - Existing user messages for zero aired episodes remain unchanged
  </behavior>
  <action>
In `markSeasonWatched(season: Int)`, route series content through `tvMetadataRouter.fetchSeasonEpisodes(contentId = meta.id, fallbackContentId = itemId, seasonNumber = season, language = null)` before TMDB ID resolution. Use returned `TvSeasonEpisode` values to build `SeasonEpisodeMark(episodeNumber = episode.episodeNumber, airDate = episode.airDate)` after applying `AirDateGate.isAired(0L, episode.airDate, nowMs)`.

Keep existing TMDB code as movie fallback or explicit TV fallback inside the router result. Do not replace `AirDateGate` and do not compute exact `airsTime`; Phase 8 owns exact air-time gating.
  </action>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.ui.screens.detail.MarkSeasonWatchedTest"</automated>
  </verify>
  <acceptance_criteria>
    - `MetaDetailsViewModel.kt` contains `tvMetadataRouter.fetchSeasonEpisodes`
    - `MetaDetailsViewModel.kt` still contains `AirDateGate.isAired(0L`
    - `MarkSeasonWatchedTest.kt` contains `uses TVDB season episodes when TVDB succeeds`
    - `MarkSeasonWatchedTest.kt` contains `coVerify(exactly = 0) { tmdbMetadataService.fetchSeasonEpisodes`
    - Targeted Gradle test command exits 0
  </acceptance_criteria>
  <done>Detail season actions consume TVDB episode lists without changing date-only gating semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route params/SavedStateHandle -> Detail provider routing | `itemId`, `itemType`, and `Meta.id` are untrusted route/catalog values |
| `TvMetadataRouter` -> Detail UI models | Provider data updates `Meta` and `Video` shown to the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | Detail logs/tests | mitigate | Do not log TVDB API key/PIN/token; tests use mocked `TvMetadataRouter` values only |
| T-07-02 | Repudiation | `MetaDetailsViewModel.enrichMeta` | mitigate | Route all series metadata through `TvMetadataRouter` so fallback/skipped-TMDB diagnostics originate centrally |
| T-07-03 | Tampering | Detail cache/provider output | mitigate | Detail must consume provider-neutral `TvMetadataEnrichment`; it must not write TVDB data into TMDB cache namespaces |
| T-07-04 | Tampering | Detail ID routing | mitigate | Do not call `tmdbService.ensureTmdbId` for series before `TvMetadataRouter` handles TVDB identity resolution |
</threat_model>

<verification>
Run:
```sh
./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.ui.screens.detail.MetaDetailsTvdbProviderRoutingTest" --tests "com.nexio.tv.ui.screens.detail.MarkSeasonWatchedTest"
```
</verification>

<success_criteria>
- Representative Detail test proves TVDB success skips TMDB TV metadata calls.
- Series detail `Meta` and `Video` roles are still populated through existing fields.
- Movie detail behavior remains TMDB-backed.
- Phase 8 exact air-time and Phase 9 advanced surfaces are not implemented here.
</success_criteria>

<output>
After completion, create `.planning/phases/07-tvdb-provider-replacement/07-03-SUMMARY.md`.
</output>
