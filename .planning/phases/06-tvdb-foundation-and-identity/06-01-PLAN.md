---
phase: 06-tvdb-foundation-and-identity
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - app/src/test/java/com/nexio/tv/core/tvdb/TvdbAuthServiceTest.kt
  - app/src/test/java/com/nexio/tv/core/tvdb/TvdbIdentityServiceTest.kt
  - app/src/test/java/com/nexio/tv/core/tvdb/TvdbProviderFallbackTest.kt
  - app/src/test/java/com/nexio/tv/core/tvdb/TvdbDiagnosticsTest.kt
  - app/src/test/java/com/nexio/tv/ui/screens/settings/TvdbSettingsViewModelTest.kt
  - app/src/test/java/com/nexio/tv/core/sync/AccountConfigSyncContractTest.kt
autonomous: true
requirements:
  - PREF-01
  - PREF-04
  - PREF-05
  - PREF-06
  - CACHE-01
user_setup: []

must_haves:
  truths:
    - "Automated RED coverage exists before TVDB auth, identity, settings, sync, and fallback implementation begins"
    - "Tests prove blank PIN is omitted from TVDB /login while non-blank PIN is sent"
    - "Tests prove public account sync contains TVDB enabled/configured/status only and no apiKey, pin, or token fields"
    - "Tests prove TVDB identity accepts TVDB, IMDb, TMDB, TV Maze, Wikidata, and official-site IDs without requiring TmdbApi"
    - "Tests prove inactive TVDB preserves non-TVDB behavior and active unusable TVDB records a diagnostic fallback reason"
  artifacts:
    - path: "app/src/test/java/com/nexio/tv/core/tvdb/TvdbAuthServiceTest.kt"
      provides: "TVDB /login, token cache, blank PIN, and 401 coverage"
      contains: "blank PIN is omitted from login payload"
    - path: "app/src/test/java/com/nexio/tv/core/tvdb/TvdbIdentityServiceTest.kt"
      provides: "Remote-ID normalization, cache, and in-flight de-duping coverage"
      contains: "official-site"
    - path: "app/src/test/java/com/nexio/tv/ui/screens/settings/TvdbSettingsViewModelTest.kt"
      provides: "Settings validation state and enablement gating coverage"
      contains: "Fallback active"
    - path: "app/src/test/java/com/nexio/tv/core/sync/AccountConfigSyncContractTest.kt"
      provides: "TVDB public sync and credential omission coverage"
      contains: "tvdb"
  key_links:
    - from: "TvdbAuthServiceTest"
      to: "TvdbAuthService"
      via: "MockWebServer or MockK login response"
      pattern: "TvdbAuthService"
    - from: "TvdbIdentityServiceTest"
      to: "TvdbIdentityService"
      via: "Concurrent lookups join one TVDB remote-id request"
      pattern: "coVerify\\(exactly = 1\\).*searchByRemoteId"
    - from: "AccountConfigSyncContractTest"
      to: "AccountConfigSyncPayload.integrations.tvdb"
      via: "kotlinx serialization JSON assertions"
      pattern: "integrations.*tvdb"
---

<objective>
Create the Phase 6 automated validation surface before implementation.

Purpose: Preserve the validation map from `06-VALIDATION.md` and force TVDB credentials, identity matching, sync secrecy, fallback diagnostics, and cache behavior to be implemented against executable tests.
Output: RED unit tests and sync contract assertions that downstream plans must turn green.
</objective>

<execution_context>
@/Users/jneerdael/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jneerdael/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-tvdb-foundation-and-identity/06-CONTEXT.md
@.planning/phases/06-tvdb-foundation-and-identity/06-RESEARCH.md
@.planning/phases/06-tvdb-foundation-and-identity/06-VALIDATION.md
@tvdb.yml

<interfaces>
Use existing test style from:
- `app/src/test/java/com/nexio/tv/ui/screens/settings/TheIntroDbSettingsViewModelTest.kt`: `StandardTestDispatcher`, `Dispatchers.setMain`, `advanceUntilIdle`.
- `app/src/test/java/com/nexio/tv/core/tmdb/TmdbMetadataPerformanceTest.kt`: MockK `coEvery` / `coVerify(exactly = 1)` for in-flight de-duping.
- `app/src/test/java/com/nexio/tv/core/sync/AccountConfigSyncContractTest.kt`: kotlinx serialization JSON-object assertions for account sync payload shape.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED TVDB auth and identity tests</name>
  <files>app/src/test/java/com/nexio/tv/core/tvdb/TvdbAuthServiceTest.kt, app/src/test/java/com/nexio/tv/core/tvdb/TvdbIdentityServiceTest.kt</files>
  <read_first>
    app/src/test/java/com/nexio/tv/core/tmdb/TmdbMetadataPerformanceTest.kt
    app/src/main/java/com/nexio/tv/core/tmdb/TmdbService.kt
    app/src/main/java/com/nexio/tv/core/di/NetworkModule.kt
    app/src/main/java/com/nexio/tv/data/remote/api/TmdbApi.kt
    tvdb.yml
    .planning/phases/06-tvdb-foundation-and-identity/06-VALIDATION.md
  </read_first>
  <behavior>
    - Test: `TvdbAuthService.loginAndCacheToken` sends JSON with `apikey` and omits `pin` when the trimmed PIN is blank, per D-01.
    - Test: `TvdbAuthService.loginAndCacheToken` sends non-blank `pin`, persists token plus expiry metadata, and returns a bearer token without logging the token, per D-01 and D-13.
    - Test: 401 from `/login` returns invalid credential state and does not persist a cached token, per D-07.
    - Test: two concurrent `resolveSeriesByRemoteId("tt0944947", IMDB)` calls share one `TvdbApi.searchByRemoteId` call, per D-15.
    - Test: identity lookup preserves normalized `TVDB`, `IMDB`, `TMDB`, `TV_MAZE`, `WIKIDATA`, `OFFICIAL_SITE`, and `OTHER` remote IDs, per D-10 through D-12.
  </behavior>
  <action>Create the two test files with exact package `com.nexio.tv.core.tvdb`. Use JUnit4, MockK, kotlinx-coroutines-test, and Retrofit `Response.success`. Tests may refer to production classes that do not exist yet: `TvdbAuthService`, `TvdbTokenStore`, `TvdbApi`, `TvdbLoginRequest`, `TvdbLoginResponse`, `TvdbIdentityService`, `TvdbRemoteIdSource`, and `TvdbSeriesIdentity`. Do not add implementation code in this task.</action>
  <acceptance_criteria>
    - `grep -n "blank PIN is omitted" app/src/test/java/com/nexio/tv/core/tvdb/TvdbAuthServiceTest.kt` returns a matching test name.
    - `grep -n "coVerify(exactly = 1).*searchByRemoteId" app/src/test/java/com/nexio/tv/core/tvdb/TvdbIdentityServiceTest.kt` returns a matching assertion.
    - `grep -n "OFFICIAL_SITE\\|WIKIDATA\\|TV_MAZE" app/src/test/java/com/nexio/tv/core/tvdb/TvdbIdentityServiceTest.kt` returns all three source names.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.core.tvdb.TvdbAuthServiceTest" --tests "com.nexio.tv.core.tvdb.TvdbIdentityServiceTest" # expected RED until Plans 06-02 and 06-03 implement the referenced classes</automated>
  </verify>
  <done>RED auth and identity tests exist and fail only because the planned TVDB production classes or methods are not implemented yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RED TVDB settings, sync, and fallback tests</name>
  <files>app/src/test/java/com/nexio/tv/ui/screens/settings/TvdbSettingsViewModelTest.kt, app/src/test/java/com/nexio/tv/core/sync/AccountConfigSyncContractTest.kt, app/src/test/java/com/nexio/tv/core/tvdb/TvdbProviderFallbackTest.kt, app/src/test/java/com/nexio/tv/core/tvdb/TvdbDiagnosticsTest.kt</files>
  <read_first>
    app/src/test/java/com/nexio/tv/ui/screens/settings/TheIntroDbSettingsViewModelTest.kt
    app/src/test/java/com/nexio/tv/core/sync/AccountConfigSyncContractTest.kt
    app/src/main/java/com/nexio/tv/ui/screens/settings/TmdbSettingsViewModel.kt
    app/src/main/java/com/nexio/tv/data/remote/supabase/AccountSyncModels.kt
    app/src/main/java/com/nexio/tv/core/sync/AccountConfigSyncContract.kt
    .planning/phases/06-tvdb-foundation-and-identity/06-CONTEXT.md
    .planning/phases/06-tvdb-foundation-and-identity/06-VALIDATION.md
  </read_first>
  <behavior>
    - Test: enabling TVDB with no API key keeps provider inactive and emits `TvdbValidationError.MissingApiKey`, per D-04 and PREF-01.
    - Test: invalid credentials set status `INVALID` with last failure text and do not enable provider use, per D-07 and PREF-05.
    - Test: successful validation sets status `VALID`, masks API key display outside dialog, and never exposes PIN, per D-02 and D-04.
    - Test: `AccountConfigSyncPayload.integrations.tvdb` serializes `enabled`, `configured`, `validationStatus`, and `lastFailure` while JSON omits `apiKey`, `pin`, and `token`, per D-02 and D-03.
    - Test: inactive TVDB resolves to TMDB/non-TVDB fallback without changing existing TMDB settings and active unusable TVDB records a sanitized fallback diagnostic, per D-08 and PREF-04.
  </behavior>
  <action>Create the three new TVDB test files and extend `AccountConfigSyncContractTest.kt` with TVDB cases. Use exact expected public fields `enabled`, `configured`, `validationStatus`, and `lastFailure`. Use exact secret type string `tvdb_api_key` and secret ref `integration:tvdb` in assertions. Do not include any literal real credential values; use test strings `tvdb-key` and `subscriber-pin` only.</action>
  <acceptance_criteria>
    - `grep -n "MissingApiKey\\|InvalidCredentials\\|Fallback active" app/src/test/java/com/nexio/tv/ui/screens/settings/TvdbSettingsViewModelTest.kt` returns matches.
    - `grep -n "apiKey\\|pin\\|token" app/src/test/java/com/nexio/tv/core/sync/AccountConfigSyncContractTest.kt` returns assertions proving those fields are absent from public JSON.
    - `grep -n "tvdb_api_key\\|integration:tvdb" app/src/test/java/com/nexio/tv/core/sync/AccountConfigSyncContractTest.kt` returns matches.
    - `grep -n "inactive TVDB leaves TMDB" app/src/test/java/com/nexio/tv/core/tvdb/TvdbProviderFallbackTest.kt` returns a matching test name.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.ui.screens.settings.TvdbSettingsViewModelTest" --tests "com.nexio.tv.core.sync.AccountConfigSyncContractTest" --tests "com.nexio.tv.core.tvdb.TvdbProviderFallbackTest" --tests "com.nexio.tv.core.tvdb.TvdbDiagnosticsTest" # expected RED until Plans 06-02, 06-04, and 06-05 implement the referenced contracts</automated>
  </verify>
  <done>RED settings, sync, fallback, and diagnostics tests exist with exact Phase 6 assertions and no real credentials.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TVDB credential input to app state | API key and optional PIN move from settings UI into local stores and TVDB login request. |
| App to TVDB API | Bearer token is obtained and sent over Retrofit headers. |
| App to account sync | Public settings and private secret payloads are split before Supabase RPC calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Information Disclosure | TvdbAuthServiceTest | mitigate | Tests assert blank PIN omission, no token/API key/PIN in public JSON, and no real credential literals. |
| T-06-01-02 | Denial of Service | TvdbIdentityServiceTest | mitigate | Tests require concurrent identity calls to join a single remote request. |
| T-06-01-03 | Tampering | TvdbIdentityServiceTest | mitigate | Tests require series-only identity results and broad remote-ID preservation. |
| T-06-01-04 | Information Disclosure | AccountConfigSyncContractTest | mitigate | Tests assert public sync omits `apiKey`, `pin`, and `token`. |
</threat_model>

<verification>
After all Phase 6 implementation plans complete, run:
- `./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.core.tvdb.*" --tests "com.nexio.tv.ui.screens.settings.TvdbSettingsViewModelTest" --tests "com.nexio.tv.core.sync.AccountConfigSyncContractTest"`
- `./gradlew testArm64DebugUnitTest`
</verification>

<success_criteria>
All Phase 6 missing test files from `06-VALIDATION.md` exist, test names and assertions match the validation map, and downstream implementation plans have a concrete RED target for every Phase 6 requirement.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tvdb-foundation-and-identity/06-01-SUMMARY.md`
</output>
