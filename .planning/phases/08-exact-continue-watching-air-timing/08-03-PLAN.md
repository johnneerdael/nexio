---
phase: 08-exact-continue-watching-air-timing
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - app/src/main/java/com/nexio/tv/data/local/ContinueWatchingSnapshotStore.kt
  - app/src/main/java/com/nexio/tv/data/repository/ContinueWatchingTimeline.kt
  - app/src/main/java/com/nexio/tv/ui/screens/home/HomeViewModelContinueWatching.kt
  - app/src/main/java/com/nexio/tv/core/recommendations/AndroidTvFeedCatalogService.kt
  - app/src/test/java/com/nexio/tv/data/local/ContinueWatchingSnapshotStoreTest.kt
  - app/src/test/java/com/nexio/tv/data/repository/ContinueWatchingTimelineAirDateTest.kt
  - app/src/test/java/com/nexio/tv/core/recommendations/AndroidTvFeedCatalogServiceContinueWatchingTest.kt
autonomous: true
requirements:
  - AIR-03
  - AIR-04
  - AIR-05
  - AIR-06
user_setup: []
must_haves:
  truths:
    - "Exact future next-up rows are absent from main Continue Watching, Trakt up-next, and Android TV feed rows until available"
    - "Already-started resume rows remain visible regardless of future next-up timing"
    - "Withheld rows and timing diagnostics persist across process restart through the snapshot store"
    - "TV detail episode lists are not filtered by the Continue Watching exact-air-time gate"
  artifacts:
    - path: "app/src/main/java/com/nexio/tv/data/local/ContinueWatchingSnapshotStore.kt"
      provides: "Schema-versioned persistence for scheduledReemit and timing fields"
      contains: "scheduledReemit"
    - path: "app/src/main/java/com/nexio/tv/data/repository/ContinueWatchingTimeline.kt"
      provides: "Shared exact-timing split logic for Continue Watching rails"
      contains: "availabilityInstantMs"
    - path: "app/src/main/java/com/nexio/tv/core/recommendations/AndroidTvFeedCatalogService.kt"
      provides: "Android TV feed inherits exact gate through snapshot/timeline refs"
      contains: "tvdbAvailabilityInstantMs"
  key_links:
    - from: "app/src/main/java/com/nexio/tv/data/local/ContinueWatchingSnapshotStore.kt"
      to: "app/src/main/java/com/nexio/tv/data/repository/ContinueWatchingSnapshotService.kt"
      via: "ContinueWatchingSnapshot.scheduledReemit persistence"
      pattern: "scheduledReemit"
    - from: "app/src/main/java/com/nexio/tv/ui/screens/home/HomeViewModelContinueWatching.kt"
      to: "app/src/main/java/com/nexio/tv/data/repository/AirDateGate.kt"
      via: "NextUpInfo.hasAired exact check"
      pattern: "tvdbAvailabilityInstantMs"
---

<objective>
Persist withheld exact-timing rows and make all Continue Watching consumers pass exact availability into the shared gate.

Purpose: Exact timing must survive app restart and apply consistently to the in-app rail, Trakt up-next rail, and Android TV recommendations/feed while leaving detail screens outside the gate.
Output: Snapshot schema bump, exact-aware timeline refs, Home mapping update, Android TV feed update, and tests.
</objective>

<execution_context>
@/Users/jneerdael/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jneerdael/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-exact-continue-watching-air-timing/08-CONTEXT.md
@.planning/phases/08-exact-continue-watching-air-timing/08-RESEARCH.md
@.planning/phases/08-exact-continue-watching-air-timing/08-VALIDATION.md
@app/src/main/java/com/nexio/tv/data/local/ContinueWatchingSnapshotStore.kt
@app/src/main/java/com/nexio/tv/data/repository/ContinueWatchingTimeline.kt
@app/src/main/java/com/nexio/tv/ui/screens/home/HomeViewModelContinueWatching.kt
@app/src/main/java/com/nexio/tv/core/recommendations/AndroidTvFeedCatalogService.kt
@app/src/test/java/com/nexio/tv/data/local/ContinueWatchingSnapshotStoreTest.kt
@app/src/test/java/com/nexio/tv/data/repository/ContinueWatchingTimelineAirDateTest.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 persistence and surface gating tests</name>
  <files>
    app/src/test/java/com/nexio/tv/data/local/ContinueWatchingSnapshotStoreTest.kt,
    app/src/test/java/com/nexio/tv/data/repository/ContinueWatchingTimelineAirDateTest.kt,
    app/src/test/java/com/nexio/tv/core/recommendations/AndroidTvFeedCatalogServiceContinueWatchingTest.kt
  </files>
  <read_first>
    app/src/test/java/com/nexio/tv/data/local/ContinueWatchingSnapshotStoreTest.kt,
    app/src/test/java/com/nexio/tv/data/repository/ContinueWatchingTimelineAirDateTest.kt,
    app/src/test/java/com/nexio/tv/core/recommendations/AndroidTvOwnedChannelRowsTest.kt,
    app/src/main/java/com/nexio/tv/data/local/ContinueWatchingSnapshotStore.kt,
    app/src/main/java/com/nexio/tv/data/repository/ContinueWatchingTimeline.kt,
    app/src/main/java/com/nexio/tv/core/recommendations/AndroidTvFeedCatalogService.kt,
    .planning/phases/08-exact-continue-watching-air-timing/08-VALIDATION.md
  </read_first>
  <behavior>
    - Store persists `scheduledReemit` and exact timing fields
    - Timeline excludes exact future next-up rows from main feed while keeping synthetic candidates for later filtering
    - Resume rows remain visible when a future next-up row for the same show is withheld, per D-12
    - Android TV Continue Watching feed uses only visible snapshot rows and does not re-add `scheduledReemit`, per D-11 and D-22
  </behavior>
  <action>
Extend `ContinueWatchingSnapshotStoreTest.kt` with exact test name `write persists scheduled reemit and tvdb timing fields`.

Extend `ContinueWatchingTimelineAirDateTest.kt` with exact test names:
- `allRailsAirDateGated - exact future nextUp entries are excluded`
- `allRailsAirDateGated - resume items remain visible when next up is exact future`

Create `AndroidTvFeedCatalogServiceContinueWatchingTest.kt` in package `com.nexio.tv.core.recommendations` with exact test name `continue watching feed does not include scheduled reemit rows`. The test may call an `internal` helper extracted from `AndroidTvFeedCatalogService` if constructing the whole service is too heavy; if extracting a helper, name it `buildContinueWatchingItemsForAndroidTvFeed`.
  </action>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.data.local.ContinueWatchingSnapshotStoreTest" --tests "com.nexio.tv.data.repository.ContinueWatchingTimelineAirDateTest" --tests "com.nexio.tv.core.recommendations.AndroidTvFeedCatalogServiceContinueWatchingTest"</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/test/java/com/nexio/tv/data/local/ContinueWatchingSnapshotStoreTest.kt` contains `write persists scheduled reemit and tvdb timing fields`
    - `app/src/test/java/com/nexio/tv/data/repository/ContinueWatchingTimelineAirDateTest.kt` contains `exact future nextUp entries are excluded`
    - `ContinueWatchingTimelineAirDateTest.kt` contains `resume items remain visible`
    - `app/src/test/java/com/nexio/tv/core/recommendations/AndroidTvFeedCatalogServiceContinueWatchingTest.kt` exists
    - `AndroidTvFeedCatalogServiceContinueWatchingTest.kt` contains `does not include scheduled reemit rows`
  </acceptance_criteria>
  <done>Wave 0 tests cover persistence, rail gating, resume visibility, and Android TV feed behavior before production consumers change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist scheduledReemit and TVDB timing fields</name>
  <files>
    app/src/main/java/com/nexio/tv/data/local/ContinueWatchingSnapshotStore.kt,
    app/src/test/java/com/nexio/tv/data/local/ContinueWatchingSnapshotStoreTest.kt
  </files>
  <read_first>
    app/src/main/java/com/nexio/tv/data/local/ContinueWatchingSnapshotStore.kt,
    app/src/main/java/com/nexio/tv/data/repository/TrackingProgressService.kt,
    app/src/main/java/com/nexio/tv/core/tvdb/TvdbAirAvailability.kt,
    app/src/test/java/com/nexio/tv/data/local/ContinueWatchingSnapshotStoreTest.kt
  </read_first>
  <action>
In `ContinueWatchingSnapshotStore`, bump `SCHEMA_VERSION` from `4` to `5`.

In `write`, add `add("scheduledReemit", encodeNextUpItems(snapshot.scheduledReemit))`.

In `decode`, set `scheduledReemit = decodeNextUpItems(root, "scheduledReemit")` on the canonical `ContinueWatchingSnapshot`.

In `encodeNextUpItems`, write the six new timing fields only when present/non-default:
- `tvdbAvailabilityInstantMs`
- `tvdbAvailabilityPrecision`
- `tvdbAvailabilitySourceZoneId`
- `tvdbAvailabilitySourcePolicy`
- `tvdbAvailabilityDiagnosticReason`
- `tvdbAvailabilityDeviceLocalDateTime`

In `decodeNextUpItemObject`, read the same fields and map enum names with `runCatching { TvdbAirAvailabilityPrecision.valueOf(value) }` and `runCatching { TvdbAirAvailabilityDiagnosticReason.valueOf(value) }`; invalid enum strings must fall back to `UNKNOWN` or `null` without throwing.
  </action>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.data.local.ContinueWatchingSnapshotStoreTest"</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/main/java/com/nexio/tv/data/local/ContinueWatchingSnapshotStore.kt` contains `SCHEMA_VERSION = 5`
    - `ContinueWatchingSnapshotStore.kt` contains `add("scheduledReemit", encodeNextUpItems(snapshot.scheduledReemit))`
    - `ContinueWatchingSnapshotStore.kt` contains `decodeNextUpItems(root, "scheduledReemit")`
    - `ContinueWatchingSnapshotStore.kt` contains `tvdbAvailabilityInstantMs`
    - `ContinueWatchingSnapshotStore.kt` contains `tvdbAvailabilityPrecision`
    - `ContinueWatchingSnapshotStore.kt` contains `TvdbAirAvailabilityDiagnosticReason.valueOf`
    - Targeted Gradle test command exits 0
  </acceptance_criteria>
  <done>Withheld next-up entries and exact timing metadata survive snapshot persistence and restore.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pass exact availability through timeline, Home, and Android TV feed refs</name>
  <files>
    app/src/main/java/com/nexio/tv/data/repository/ContinueWatchingTimeline.kt,
    app/src/main/java/com/nexio/tv/ui/screens/home/HomeViewModelContinueWatching.kt,
    app/src/main/java/com/nexio/tv/core/recommendations/AndroidTvFeedCatalogService.kt,
    app/src/test/java/com/nexio/tv/data/repository/ContinueWatchingTimelineAirDateTest.kt,
    app/src/test/java/com/nexio/tv/core/recommendations/AndroidTvFeedCatalogServiceContinueWatchingTest.kt
  </files>
  <read_first>
    app/src/main/java/com/nexio/tv/data/repository/ContinueWatchingTimeline.kt,
    app/src/main/java/com/nexio/tv/ui/screens/home/HomeViewModelContinueWatching.kt,
    app/src/main/java/com/nexio/tv/core/recommendations/AndroidTvFeedCatalogService.kt,
    app/src/main/java/com/nexio/tv/data/repository/AirDateGate.kt,
    app/src/test/java/com/nexio/tv/data/repository/ContinueWatchingTimelineAirDateTest.kt,
    app/src/test/java/com/nexio/tv/core/recommendations/AndroidTvFeedCatalogServiceContinueWatchingTest.kt
  </read_first>
  <action>
Update `ContinueWatchingNextUpRef` to include `val availabilityInstantMs: Long? = null`.

Update `splitNextUpCandidatesForContinueWatching` so `AirDateGate.isAired(availabilityInstantMs = ref.availabilityInstantMs, firstAiredMs = ref.firstAiredMs, tmdbAirDate = null, nowMs = nowMs)` is used for main-feed filtering.

Update these ref builders to pass `entry.tvdbAvailabilityInstantMs`:
- `ContinueWatchingSnapshotService.nextUpRefForEntry`
- `HomeViewModelContinueWatching.nextUpRefForContinueWatching`
- `AndroidTvFeedCatalogService.nextUpRefForAndroidTvFeed`

Update `HomeViewModelContinueWatching.toContinueWatchingNextUp` so `hasAired` calls the four-argument `AirDateGate.isAired(entry.tvdbAvailabilityInstantMs, firstAiredMs, firstAired, nowMs)`.

If `AndroidTvFeedCatalogServiceContinueWatchingTest` needs access to feed item building, extract the private method body into an `internal fun buildContinueWatchingItemsForAndroidTvFeed(snapshot: ContinueWatchingSnapshot): List<MetaPreview>` in the same file and have `buildContinueWatchingItems` delegate to it. Do not add any Continue Watching placeholder row for `snapshot.scheduledReemit`.

Do not modify files under `app/src/main/java/com/nexio/tv/ui/screens/detail/` in this task; AIR-06 requires detail episode lists to remain outside this Continue Watching gate.
  </action>
  <verify>
    <automated>./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.data.repository.ContinueWatchingTimelineAirDateTest" --tests "com.nexio.tv.core.recommendations.AndroidTvFeedCatalogServiceContinueWatchingTest"</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/main/java/com/nexio/tv/data/repository/ContinueWatchingTimeline.kt` contains `availabilityInstantMs: Long? = null`
    - `ContinueWatchingTimeline.kt` contains `availabilityInstantMs = ref.availabilityInstantMs`
    - `app/src/main/java/com/nexio/tv/ui/screens/home/HomeViewModelContinueWatching.kt` contains `tvdbAvailabilityInstantMs`
    - `app/src/main/java/com/nexio/tv/core/recommendations/AndroidTvFeedCatalogService.kt` contains `tvdbAvailabilityInstantMs`
    - `AndroidTvFeedCatalogService.kt` does not contain `scheduledReemit.map`
    - `git diff -- app/src/main/java/com/nexio/tv/ui/screens/detail` produces no diff from this task
    - Targeted Gradle test command exits 0
  </acceptance_criteria>
  <done>All Continue Watching consumers pass exact availability into the shared gate, and detail screens remain unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Snapshot store -> app startup | Persisted JSON can be stale, malformed, or manually edited |
| Snapshot/timeline -> UI and Android TV feed | Gated rows decide what user-facing surfaces display |
| Scheduled withheld rows -> diagnostics | Withheld row metadata includes timing reason and computed local time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Tampering / Denial of Service | `ContinueWatchingSnapshotStore.decodeNextUpItemObject` | mitigate | Parse new optional fields with `runCatching`; reject invalid required IDs/season/episode as before and never throw on enum mismatch |
| T-08-10 | Information Disclosure | Snapshot persistence | mitigate | Persist timing reason/source/local time only; do not persist TVDB credentials, bearer tokens, raw request payloads, or auth headers |
| T-08-11 | Integrity | Home/Android TV feed refs | mitigate | Use the same `AirDateGate` path for main rail, Trakt up-next, and Android TV feed; do not duplicate gate logic |
| T-08-12 | Integrity | Detail screen boundary | mitigate | Keep detail files unchanged so future unaired episode lists remain visible while Continue Watching rows are gated |
</threat_model>

<verification>
Run:
```sh
./gradlew testArm64DebugUnitTest --tests "com.nexio.tv.data.local.ContinueWatchingSnapshotStoreTest" --tests "com.nexio.tv.data.repository.ContinueWatchingTimelineAirDateTest" --tests "com.nexio.tv.core.recommendations.AndroidTvFeedCatalogServiceContinueWatchingTest"
```
</verification>

<success_criteria>
- AIR-03: future exact-timing rows are withheld from Continue Watching surfaces until available.
- AIR-04: withheld rows are persisted so durable scheduling can reschedule after restart.
- AIR-05: date-only diagnostic fields persist.
- AIR-06: detail screens are not filtered by Continue Watching gating.
</success_criteria>

<output>
After completion, create `.planning/phases/08-exact-continue-watching-air-timing/08-03-SUMMARY.md`.
</output>
