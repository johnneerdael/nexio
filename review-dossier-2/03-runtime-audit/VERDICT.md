# Generated Gate Verdicts — Snapshot at SHA `774a540f8`

Generated by dossier prep on 2026-04-29.

| Gradle task | Bundle verdict | Gradle exit | Notes |
|---|---|---|---|
| `generateProfileBoundaryAudit` | `PASS` | green | 5 scenarios, 0 violations |
| `generateIntegrationRuntimeAudit` | `PASS` | green | Control plane PASS; MetadataRouter readiness PASS_WITH_WARNINGS; 24 providers, 127 endpoint shapes, 93 runtime-covered calls, 0 direct-bypass calls, 0 missing policies, 0 endpoint-shape mismatches, 0 missing header policies, 0 missing operation keys, 0 active required runtime specs missing, 46 planned-not-active shapes |
| `generateMetadataExecutionAudit` | `PASS` (bundle) but **GRADLE TASK FAILED** (1 downstream test) | red | Bundle verdict is `PASS` (`SIGN_OFF_AGGREGATE`, 30 items, 23 routed, 17 network, 7 cache hits, 1 stale hit, 5 forbidden overwrites, 0 policy violations). The `MetadataExecutionAuditGoldenTest.routing rules match spec for all id types` test fails: `tt14403178` (IMDB series) routes with reason `ROUTING_ID_TYPE_CONFLICT` but the golden expects `ITEM_TYPE_SERIES`. Behavior change introduced by parallel-session commit `4427f22ed` ("Resolve IMDb series through TVDB remote IDs"). See lane B for full finding. |
| `generateTraceValidatorAudit` | `PASS` | green | 11 tests across 5 validator suites, 0 failures (LocalizationPlan, RealEmission, ScheduledDispatched, RuntimeTrace generic, TraceBundleGolden) |

## SHA inconsistency note

The audit reports stamp `Git SHA: 9f0555a5a` and worktree `DIRTY (4 changed files, 3 untracked)`. The actual current branch tip when running these audits was `774a540f8`. This implies the audit's git-sha capture mechanism reads from a cached source (e.g. a build/ artifact captured before the build/ directory was touched), or it derives from a different base. **Finding to investigate as part of lane I (trace mode integrity).**

## Files in this directory

- `integration-runtime-audit.md` — full markdown report
- `integration-runtime-audit.json` — machine-readable
- `boundary-violations.csv` — empty (PASS)
- `cache-policy-violations.csv` — empty (PASS)
- `header-violations.csv` — empty (PASS)

Sibling directories:
- `../04-metadata-execution-audit/` — bundle MD/JSON + the failing test's XML
- `../05-profile-boundary-audit/` — boundary report MD/JSON
- `../06-localization-audit/` — trace validator test XMLs
