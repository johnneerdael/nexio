<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <queries>
        <package android:name="com.netflix.ninja" />
        <package android:name="com.netflix.mediaclient" />
        <package android:name="com.amazon.amazonvideo.livingroom" />
        <package android:name="com.amazon.avod.thirdpartyclient" />
        <package android:name="com.disney.disneyplus" />
        <package android:name="com.wbd.stream" />
        <package android:name="com.hulu.livingroomplus" />
        <package android:name="com.hulu.plus" />
        <package android:name="com.apple.atve.androidtv.appletv" />
        <package android:name="com.peacocktv.peacockandroid" />
        <package android:name="com.crunchyroll.crunchyroid" />
    </queries>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="com.android.providers.tv.permission.WRITE_EPG_DATA" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.faketouch"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <application
        android:name=".NexioApplication"
        android:allowBackup="true"
        android:banner="@drawable/tv_banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:localeConfig="@xml/locale_config"
        android:theme="@style/Theme.MyApplication">
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.MyApplication.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".debug.passthrough.TransportValidationReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.nexio.tv.DEBUG_PASSTHROUGH_VALIDATION" />
            </intent-filter>
        </receiver>

        <!--
            Playback-trace diagnostics ADB control plane.

            Exported = true so `adb shell am broadcast` can reach it
            without a custom permission. Every action is gated at
            runtime by `PlaybackTraceAdbControlToggle` — the user must
            explicitly enable "Allow ADB control" in the debrid settings
            menu before this receiver does anything. Off by default.

            See `docs/instrumentation/collecting-traces.md` for command
            examples.
        -->
        <receiver
            android:name="com.nexio.tv.instrumentation.PlaybackTraceAdbReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.nexio.tv.action.PLAYBACK_TRACE_ENABLE" />
                <action android:name="com.nexio.tv.action.PLAYBACK_TRACE_DISABLE" />
                <action android:name="com.nexio.tv.action.PLAYBACK_TRACE_STATUS" />
                <action android:name="com.nexio.tv.action.PLAYBACK_TRACE_EXPORT_LAST" />
                <action android:name="com.nexio.tv.action.PLAYBACK_TRACE_EXPORT_ALL" />
                <action android:name="com.nexio.tv.action.PLAYBACK_TRACE_CLEAR" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
